# AlgoShowMaker 開發驗收

- 開始修改前檢查 `git status`，保留既有修改。
- 驗證依使用者最新指定的分級執行，開始前讀取根目錄 `子代理驗證分級通知.md`。
- 非動畫的前端／投影片修改不啟動演算法驗證集；執行必要語法、差異及局部行為確認，相關小測試按需選用。
- 開發／子代理只執行相關分類的最小小驗證，不得執行完整 regression、全部 tests、廣泛排序整合或大規模演算法動畫驗證。
- 主代理負責核實、合併與最後驗收；涉及動畫的整合按影響範圍決定完整或相關演算法驗證。`npm run regression` 僅由主代理使用，不因純介面修改機械式啟動。
- 驗證失敗不得宣稱完成，也不得略過檢查或放寬容許值讓錯誤通過。
  先查看 `test-results/animation/*/summary.json` 與個別記錄，找出第一個違規時間點，
  從解析、runtime、model、排程到實際 SVG 呈現追查，修正後重跑。
- 若是環境故障（編譯器、瀏覽器或服務不可用），明確報告未完成的驗證。
- 測試使用獨立伺服器與瀏覽器，不操作使用者分頁或覆蓋使用者投影片。
- 重疊只檢查事件前後與幀前後的定點，不檢查動畫作用中的交叉；不得為此改變既有指標交換路徑。
- 修改完成後重啟本專案開發服務；既有 Docker 部署若在本次修改範圍內也重啟。
- 不提交 `test-results`、暫存編譯產物或 server log。
