# AlgoShowMaker 多代理協作設定

本文件提供給主代理與開發代理閱讀。使用者只需用自然語言描述需求；代理負責釐清、調查、撰寫任務文件、驗證與回報，不要求使用者填寫模板。

## 1. 角色與責任

- 主代理：界定任務邊界、確認共同基準、協調依賴與共用檔案、核實交付、合併分支、執行完整回歸與演算法投影片驗證。
- 開發代理：確認必要需求、重現與調查問題、完成修正及相關文件、執行小驗證、建立 commit、撰寫交付紀錄。
- 使用者：說明想要的行為，回答影響結果的必要問題；不用提供技術方案、檔案清單或測試指令。

開發代理不得自行 merge、push、發布 Release、部署公開服務、關閉 Issue，或重啟主要開發服務。這些動作由主代理依使用者授權執行。

本流程依使用者指定分工：開發代理先做相關小驗證，主代理負責合併後的大驗證。不得降低斷言、隱藏錯誤或把未驗證項目寫成通過。

驗證的分級、測試分類、檔案位置與禁止入口，以 [子代理驗證分級通知.md](子代理驗證分級通知.md) 為準。非動畫前端／投影片任務不啟動演算法驗證集，大規模演算法驗證僅由主代理執行。

## 2. 每個任務開始前，應向使用者確認什麼

先讀使用者需求與既有對話，能從上下文確定的事項不用再問。能自行查明的技術資訊，先調查，不轉交使用者回答。

只確認下列尚未清楚、且會影響結果的事項：

1. **要解決的情境**：在哪個頁面或模式、做什麼操作時發生？若使用者只說「有問題」，確認看到的現象。
2. **希望得到的結果**：修正後應如何顯示或運作？功能新增若有多種合理行為，確認使用者偏好。
3. **必要範圍與限制**：是否涉及桌面、手機、演算法頁、Trace Studio 或投影片？是否有明確必須保留的行為？只問會改變方案的範圍。
4. **重現素材**：若自行無法重現，確認必要的程式碼、輸入、deck 或操作順序。不要一開始就要求使用者準備整套證據。
5. **取捨**：若方案會改變既有行為、相容性或資料，提出具體差異與建議，再確認選擇。

只要功能會作用於已儲存物件，預設需求包含「舊物件也可直接使用」。代理不需再問使用者是否要相容；只有技術上必須做不相容變更時，才提出具體影響、遷移方案與建議供使用者決定。

提問規則：

- 一次集中問最重要的 1～3 個問題，使用一般用語，必要時給簡短選項與建議。
- 不問使用者「要修改哪個函式」「要跑哪個測試」「根因是什麼」；這些由代理查明。
- 需求已清楚就直接開始，不要求使用者再次批准完整任務文件。
- 可自行決定的實作細節寫進任務定義；影響預期結果的重要歧義必須先確認，不能以等待逾時當作同意。
- 等待回答時，可繼續不依賴答案的唯讀調查。
- 新需求或修正指示到達後，更新任務定義，不丟棄原目標，除非使用者明確取消。

提問示例：

> 你說的排序問題，是拖曳後立即順序錯誤，還是儲存後重新開啟才恢復舊順序？
> 這次希望同時支援手機拖曳嗎？若目前只處理桌面，我會先將手機列為未驗證範圍。

## 3. 工作隔離與共同基準

1. 主代理檢查 Git 狀態，保留既有修改，指定共同基準的完整 commit SHA。
2. 每個開發代理使用獨立 worktree 與 `codex/<任務代號>` 分支；同一目錄切換不同分支不算隔離。
3. 開發代理回報實際 worktree 絕對路徑、分支與 HEAD，核對確實來自指定基準。
4. 技術調查後列出預計修改的檔案與共用介面，由主代理協調重疊。
5. 如需修改其他代理負責的核心函式、資料格式或共用介面，先向主代理回報並調整分工。
6. 不自行 rebase、重設或切換其他代理的工作目錄；需要同步上游變更時由主代理協調。
7. 測試服務、埠、瀏覽器、資料與產物需隔離。不得操作使用者分頁或覆蓋使用者投影片。
8. 不把本機驗證當成公開 Docker 或遠端資料庫驗證。

本輪初始共同基準：`a524dd83b00f6ce137cf651fc7511219c2f0b772`。
後續任務仍由主代理重新指定基準，不永久沿用此 SHA。

## 4. 文件位置與維護

每個任務在自己的分支維護：

```text
docs/agent-tasks/<任務代號>/task.md
docs/agent-tasks/<任務代號>/delivery.md
```

- `task.md`：代理在需求足以開始時撰寫，隨調查與需求變更更新。
- `delivery.md`：代理交付前撰寫，主代理核實後補上整合結果。
- 兩份文件隨任務提交，使用者不用填寫。
- 只記錄與判斷、重跑相關的資訊，不抄錄整段對話或大量 log。
- 不提交 `test-results`、暫存編譯產物或 server log；證據若只留在本機，明確註記其位置與保存限制。
- 不記錄憑證、token、密碼或使用者私人資料。

## 5. 任務定義格式：task.md

以下內容由開發代理填寫。未知資訊寫「待調查」，不憑空補齊。

```markdown
# <任務代號>：<任務名稱>

## 任務資訊
- 負責代理：
- 狀態：釐清中／調查中／開發中／待交付／阻塞
- 共同基準 commit：<完整 SHA>
- 分支：codex/...
- Worktree：<絕對路徑>

## 問題與預期結果
- 情境與操作：
- 目前行為：
- 使用者希望的結果：
- 本次範圍與必要限制：

## 需求確認
- 已從使用者或上下文確認：
- 尚待使用者回答：無／具體問題
- 代理採用的合理假設：無／內容與理由

## 重現與調查
- 最小操作步驟或 fixture：
- 重現狀態：已重現／尚未重現／功能新增不適用
- 已確認事實：
- 尚待調查：

## 修改邊界與依賴
- 預計修改檔案或模組：
- 共用檔案／介面與協調結果：
- 依賴任務：無／任務代號與依賴內容

## 驗收條件
- [ ] <具體操作後應看到的結果>
- [ ] <必要的既有行為仍正常>
- [ ] <缺少本次新欄位的舊物件載入後，不需重建即可使用新功能>
- [ ] <舊物件既有內容／樣式／自訂值與明確關閉設定不被覆蓋，儲存後重開仍正確>

## 驗證計畫
- 子代理小驗證：<相關檢查、測試與實際操作>
- 舊有物件相容性：<新建、缺少新欄位、既有設定保留、持久化往返；不適用時說明理由>
- 主代理整合驗收：<依驗證層級列出相關或完整驗證；非動畫任務不啟動演算法驗證集>
- 測試隔離方式：

## 變更紀錄
- <日期>：<需求／範圍調整及依據；無調整可寫初始定義>
```

驗收條件必須可觀察、可判定。例如：

- 合適：「調整投影片順序後儲存，重新開啟仍保留同樣順序與內容。」
- 不合適：「排序正常」「提升穩定性」「完成所有 BUG 修正」。

## 6. 交付驗證紀錄格式：delivery.md

由開發代理填寫修正與小驗證部分。主代理核實區保留待填，不代替主代理宣稱完成。

```markdown
# <任務代號> 交付驗證紀錄

## 交付資訊
- 狀態：待主代理核實／有失敗或阻塞
- 分支：
- 共同基準 commit：<完整 SHA>
- 程式修正 commit：<完整 SHA 或提交清單>
- 驗證時的 HEAD 與未提交修改：
- 驗證日期：

## 根因與修改
- 已確認根因與證據：<功能新增則寫設計依據>
- 修正方式與行為變化：
- 修改檔案及用途：
- README／版本紀錄／使用說明更新：<內容或不適用理由>
- 與 task.md 的差異：無／差異與依據

## 驗收條件對照
| task.md 條件 | 驗證方式 | 實際結果 | 判定 |
|---|---|---|---|
| ... | ... | ... | 通過／失敗／未驗證 |

## 小驗證與重跑方式
### <驗證名稱>
- 目的與對應條件：
- 執行目錄與必要環境設定：
- 測試資料／fixture：
- 完整指令或操作步驟：
- 預期結果：
- 實際結果與 exit code（適用時）：
- 證據位置：<可提交 fixture／測試檔、本機摘要或截圖位置>

## 舊有物件相容性
| 案例 | Fixture／建立方式 | 操作與持久化路徑 | 實際結果 | 判定 |
|---|---|---|---|---|
| 新建物件 | ... | ... | ... | 通過／失敗／未驗證 |
| 缺少新欄位的舊物件 | ... | 載入→使用→儲存→重開 | ... | 通過／失敗／未驗證 |
| 既有自訂值／明確關閉 | ... | ... | ... | 通過／失敗／未驗證 |

## 剩餘事項與合併注意
- 未驗證項目及原因：無／內容
- 已知問題或風險：無／內容
- 相依與衝突注意：無／內容
- 主代理需補驗證的情境：

## 主代理核實與整合（由主代理填寫）
- 狀態：尚未核實
- 核實的程式 commit 與 diff 範圍：
- 差異審查與必要重跑結果：
- 合併 commit：
- 完整 regression：
- 演算法投影片實際驗證：
- 未完成或環境阻塞：
- 本機服務重啟：
- Push／公開部署狀態：
```

紀錄使用已存在的程式修正 commit，不填尚未產生的自身 commit SHA。若之後又改程式，更新驗證版本並重跑受影響項目。文件修訂可另行 commit。

## 7. 結果核實標準

- **通過**：實際執行，結果符合指定條件。
- **失敗**：實際執行，結果不符合條件；記錄最早異常與重現方法。
- **未驗證**：未執行或證據不足，寫明原因。
- **阻塞**：環境、相依或必要答案使工作無法繼續，寫明解除條件。

靜態檢查不能證明畫面正確；單一案例通過不能宣稱全部情境通過。測試結果要能對應到驗證時的程式版本。

開發代理以「小驗證通過，待主代理核實」交付，不宣稱整合驗收完成。有失敗可以回報進度或阻塞，但不能標記任務完成。

主代理依序：

1. 核對任務定義、分支基準、提交與差異範圍。
2. 審查修正是否符合需求，核實小驗證證據，必要時重跑。
3. 依任務依賴逐一合併至整合分支；處理文字衝突及行為衝突。
4. 按驗證分級驗收整合版本；純非動畫修改不啟動演算法驗證集，涉及動畫時由主代理決定相關或完整 regression，並完成相關演算法投影片實際驗證。
5. 驗證失敗時查閱摘要與記錄，從解析、runtime、model、排程追查至實際 SVG 呈現，修正後重跑。
6. 完成後更新交付紀錄、提交整合修正與文件，重啟本機開發服務。

大型回歸由主代理協調資源與執行順序。演算法重疊只驗證事件前後與幀前後的定點，不因動畫中的交叉改變原指標交換路徑。

只有整合後驗證確實通過，主代理才標記「整合驗收通過」。Push、Release 與公開部署分別記錄，不以合併或本機通過代替。

## 8. 使用者如何交辦

使用者可以直接說：

> 請依《多代理協作設定.md》處理任務 A：我在……操作時看到……，希望改成……。先確認影響結果的必要細節，其餘由你調查、撰寫任務定義與交付紀錄。使用主代理指定的基準、獨立 worktree 與分支，完成小驗證與 commit 後交付主代理。

代理不得把此模板當成要求使用者先準備完整規格的門檻。
