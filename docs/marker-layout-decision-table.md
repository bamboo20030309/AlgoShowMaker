# 指標版面、讓位與生命週期確認表

## 填寫方式

- 「已確認」代表目前已有明確規則，不需要再填。
- 「【待填】」代表會影響實作結果，請在該格最後直接補上答案。
- 案例中的 `i → heap[3]` 表示指標 `i` 指向 `heap[3]`。
- 「同格」表示兩個以上指標同時指向同一個視覺格子，不只代表索引文字相同。

## A. 身分、可見性與分組

| ID | 規則 | 具體案例 | 畫面應如何處理 | 你需要確認什麼 |
|---|---|---|---|---|
| A01 | 指標身分使用 lifetime | 函式第一次迴圈的 `now` 退場後，下一次呼叫又宣告一個 `now` | 兩個 `now` 視為不同指標，不沿用舊位置或動畫 | 已確認 |
| A02 | 相同 lifetime 延續 | `now` 從 `heap[6]` 賦值成 `parent=3`，下一幀仍是同一個 `now` | 同一個標籤從第 6 格移到第 3 格，不重新入場 | 已確認 |
| A03 | 同格判定 | `now → heap[3]`、`parent → heap[3]` | 兩者加入同一個讓位群組 | 已確認 |
| A04 | 退場前仍算可見 | `parent` 再過 200ms 才開始 scope exit，但 `now` 已移到同格 | 這 200ms 內仍替 `parent` 保留位置，兩者不能重疊 | 已確認 |
| A05 | 退場中的可見門檻 | `parent` opacity 已從 1 開始降低，但尚未完全消失 | `parent` 一開始退場就退出讓位群組；其他同格指標同時開始回填 | 已確認：退場一開始便退出。 |
| A06 | 寬格判定 | heap 根節點視覺寬 320px，一般陣列格寬 40px | 只要 `width > baseCellWidth`，就視為寬格並使用寬格的垂直箭頭規則 | 已確認：所有 `width > baseCellWidth` 都算寬格。 |
| A07 | 同格排序依據 | `i`、`j`、`parent` 同時指向 `arr[2]` | 使用固定 `sortKey` 排出左、中、右；沒有明確 `sortKey` 時使用宣告順序，避免每幀互換位置 | 已確認：固定 `sortKey`／宣告順序。 |
| A08 | reference 別名依可見範圍處理 | 區域變數 `i` 傳給 `void move(int& pos)`；進入函式後同一份資料改以 `pos` 命名 | 沿用原本指標的身分、位置、外框與箭頭，只把中央文字由 `i` 改成 `pos`；返回呼叫端時再改回 `i`。這是名稱轉換，不建立事件動畫，也不重播入場或退場 | 已確認：區域變數使用改名動畫；若 `i` 是全域變數，則可保留 `i` 並另畫 `pos`，兩者同步反映同一份資料。 |

## B. 穩定狀態排列

| ID | 規則 | 具體案例 | 畫面應如何處理 | 你需要確認什麼 |
|---|---|---|---|---|
| B01 | 單一指標置中 | 只有 `now → heap[4]` | `now` 標籤置於格子正上方中央，箭頭朝下 | 已確認 |
| B02 | 兩個指標讓位 | `now → heap[3]`、`parent → heap[3]` | 兩個標籤以格子中心左右分開，標籤框之間至少保留 8px | 已確認：最小間距為 8px。 |
| B03 | 三個指標排列 | `i`、`j`、`k` 都指向只有一格寬的 `arr[2]` | 三個標籤維持單排並沿用 8px 間距；單格上的三支箭頭都朝向格子中心，只有目標寬度大於一格時才改成垂直向下 | 已確認：單排、沿用 B02；單格箭頭朝中心，寬格箭頭垂直。 |
| B04 | 四個以上指標 | `a`、`b`、`c`、`d` 都指向同一格 | 不論數量都維持單排，依固定順序向左右展開 | 已確認：全部使用單排。 |
| B05 | 不同文字寬度 | `i` 與 `largestIndex` 指向同格；兩者的標籤框都維持正方形 | 標籤框保持正方形；名稱完整顯示，不截斷、不省略，長名稱在正方形內縮小文字以容納完整內容 | 已確認：標籤為正方形；文字完整顯示且可以縮小。 |
| B06 | 相鄰格標籤碰撞 | `left → heap[4]`、`right → heap[5]`，但兩個格子很靠近 | 目前只處理同格指標的讓位，不計算不同格之間的標籤碰撞 | 已確認：暫不處理跨格避讓。 |
| B07 | 群組水平基準 | 寬 320px 的 `heap[1]` 上有兩個短標籤 | 群組以節點中心對稱，而非以左邊第一個 40px 子區塊為基準 | 已確認 |
| B08 | 超出目前畫布視野 | canvas 左界為 x=0，最左格中心在 x=30；三個正方形標籤維持單排後，群組左緣落在 x=-60 | 保持既有的置中、單排、8px 間距與箭頭規則，不因超出目前視野而平移、縮小整組或裁改版面；使用者可移動鏡頭查看超出部分 | 已確認：標籤保持正方形並照目前規則顯示，畫布邊界不參與讓位計算。 |

## C. 指標入場

| ID | 規則 | 具體案例 | 畫面應如何處理 | 你需要確認什麼 |
|---|---|---|---|---|
| C01 | 第一個指標入場 | `int now = heapSize`，`heap[now]` 原本沒有指標 | `now` 從上方淡入，落在格子中央，不做水平讓位 | 已確認 |
| C02 | 第二個指標入場 | `parent` 已在 `heap[3]`，新 `now` 也指到 `heap[3]` | 新 `now` 入場時，`parent` 同步移到一側 | 已確認 |
| C03 | 第三個指標入場 | `i`、`j` 已在同格，`k` 新入場 | `k` 開始入場時，所有既有指標同步重排成固定順序，不能只把 `k` 塞到最外側 | 已確認：全部指標同步重排。 |
| C04 | 宣告後立即賦值 | `int now; now = heapSize;` 兩個事件連續發生 | 宣告入場完成後直接依事件順序銜接賦值移動，中間不插入額外停頓 | 已確認：不額外停頓。 |
| C05 | 同名新 lifetime 入場 | 第一次 `heap_push` 返回時舊 `now` 退場，接著第二次 `heap_push` 宣告新的 `now` | 舊 `now` 必須先完成退場，新 `now` 才開始入場；兩者維持不同身分，也不能把舊標籤瞬移成新標籤 | 已確認：生命週期事件依序完成，不重疊播放。 |
| C06 | 相鄰 lifetime 不得重疊 | 舊 `now` 的 scope exit 事件緊接著是新 `now` 的 declaration 事件，而且兩者都指向 `heap[1]` | 即使兩個事件相鄰且目標相同，也必須先完成舊 `now` 退場，再播放新 `now` 入場；畫面不會同時存在兩個 `now`，因此不需要讓位 | 已確認：嚴格按照事件順序，禁止舊 lifetime 退場與新 lifetime 入場重疊。 |
| C07 | out-of-range／unresolved 入場 | `i=-1`，或長度為 10 的陣列出現 `i=12` | 指標依索引方向顯示在結構外側的 unresolved 位置：負索引在左側，超過上界的索引在右側；首次出現時由上方淡入 | 已確認：由上方淡入；所有超出合法範圍的索引使用同一規則。 |
| C08 | 新增格與指標依序入場 | `push_back` 先新增 `heap[8]`，下一個 declaration 事件才宣告指向它的 `now` | 先完整播放 `push_back` 的格子新增與 heap 擴張動畫；動畫結束後，再依事件順序播放 `now` 入場，兩者不得重疊 | 已確認：指標不能在 `push_back` 動畫尚未結束時出現。 |

## D. 賦值與跨格移動

| ID | 規則 | 具體案例 | 畫面應如何處理 | 你需要確認什麼 |
|---|---|---|---|---|
| D01 | 空目標格 | `now: heap[6] → heap[3]`，第 3 格沒有其他指標 | `now` 沿完整 x/y 路徑移到第 3 格中央 | 已確認 |
| D02 | 目標格已有指標 | `now: heap[6] → heap[3]`，`parent` 已在第 3 格 | `now` 移入時與 `parent` 形成左右排列 | 已確認 |
| D03 | 離開來源群組 | `i`、`j` 都在 `arr[2]`，`j` 改指向 `arr[3]` | `j` 開始移動時便離開來源讓位群組，立即呼叫通用重排，讓 `i` 同步回到第 2 格中央 | 已確認：移動開始時回填。 |
| D04 | 目的群組何時讓位 | `j` 從 `arr[2]` 移向已有 `k` 的 `arr[3]` | `j` 起步時 `k` 仍留在中央；等 `j` 接近第 3 格時，才把 `j` 加入目的讓位群組並呼叫通用重排，讓 `k` 騰出位置 | 行為已確認；仍需定義「接近」的精確觸發點。 |
| D05 | 來源與目的都有多人 | `j` 從包含 `i,j` 的第 2 格移到包含 `k,l` 的第 3 格 | 移動開始時先重排來源群組，讓 `i` 回填；`j` 接近目的格時才重排目的群組，讓 `k,l` 開始讓位 | 已確認：來源先回填，目的接近時再讓位。 |
| D06 | 同一幀連續賦值 | `i=parent; i=left;` 都落在同一幀事件序列 | `i` 依序走到 parent，再走到 left，不能直接跳到 left | 已確認 |
| D07 | 賦值後另一指標立即退場 | `now` 宣告在 while 外並指向 `heap[6]`；本輪的區域變數 `parent` 指向 `heap[3]`。`now=parent` 是本輪最後一個 statement，接著 `parent` 離開 scope | 先完整播放賦值：`parent` 仍在第 3 格並參與讓位，`now` 移到自己的同格位置，賦值框結束。中間不增加停頓，下一個事件才讓 `parent` 開始退場；此時 `parent` 退出讓位群組，`now` 同步回填中央。賦值與退場不得重疊 | 已確認：沿用事件順序；退場的是 `parent`，`now` 繼續存在。 |
| D08 | 目標指標即將退場 | `now` 移入 `parent` 所在格，`parent` 稍後 scope exit | `parent` 真正退場前仍替它保留位置 | 已確認 |
| D09 | heap resize 時更新既有指標 | `now` 已指向 heap 節點，`push_back` 事件讓 outerframe 與節點配置展開 | resize 的每個 tick 都用 heap 當下幾何重新計算 `now` 的標籤、箭頭及相關 style，使它們與格子同步移動 | 已確認 |
| D10 | 由寬格移到單格 | `now: heap[1] → heap[4]` | 移動途中沿用寬格的垂直箭頭規則；`now` 抵達單格目的地後，才切換成單格箭頭規則 | 已確認：抵達目的地後才切換。 |
| D11 | 由合法格移到 out-of-range | `i: arr[0] → arr[-1]`，或長度 10 的陣列中 `i: 9 → 12` | 指標依索引方向直接平移到結構外側的 unresolved 位置，不先淡出或重新入場 | 已確認：直接平移；不論是索引賦值越界或結構縮短造成越界，都使用相同規則。 |
| D12 | unresolved 回合法格 | `i: arr[-1] → arr[0]` | `i` 從暫存位置平移回第 0 格 | 已確認 |
| D13 | 所有 marker 都代表索引 | `swap(arr[i],arr[j])` 後，資料值交換，但 `i`、`j` 仍代表原本的陣列位置 | 指標留在索引格，不跟資料值一起交換；目前不建立 value marker 類型 | 已確認：維持現狀，全部都是 index marker；未來有明確使用案例時再設計 value marker。 |

### 通用讓位函式的責任

讓位的幾何計算由共用函式負責；D03～D05 只控制何時改變群組成員並呼叫它。

- `computeMarkerLayout(group, targetBounds)`：依 `sortKey`／宣告順序、正方形標籤尺寸、8px 間距，以及單格或寬格箭頭規則，計算群組中每個指標的目標位置與箭頭端點。它只計算結果，不決定動畫時機。
- 動畫排程層：在入場、離開來源、接近目的地、退場或結構尺寸改變時，更新群組成員並呼叫共用函式，再把指標從目前位置移到新位置。
- `push_back`／`pop_back` 改變結構尺寸時，`targetBounds` 在每個動畫 tick 都會改變，因此每個 tick 都要重新呼叫版面計算；所有 style、標籤、箭頭及賦值框使用同一份當下幾何，與 outerframe 同步移動。
- 例：`j: arr[2] → arr[3]`。移動開始時先把 `j` 從第 2 格群組移除並重排 `i`；接近第 3 格時才把 `j` 加入第 3 格群組並重排 `k,l,j`。

## E. 讓位開始、持續與結束

| ID | 規則 | 具體案例 | 畫面應如何處理 | 你需要確認什麼 |
|---|---|---|---|---|
| E01 | 入場前不讓位 | `j` 的宣告事件還沒開始，但目的幀 DOM 已包含 `j` | 原有 `i` 仍置中，不能提早移開 | 已確認 |
| E02 | 入場開始才讓位 | `j` opacity 從 0 開始增加 | 同一刻 `i` 開始向旁邊移動 | 已確認 |
| E03 | 移動完成無補跳 | `j` 抵達已有 `i` 的格子 | 抵達時兩者已在最終位置，不能再突然水平跳一次 | 已確認 |
| E04 | 退場前保留位置 | `parent` 尚未開始淡出 | `now` 與 `parent` 保持兩人排列 | 已確認 |
| E05 | 退場開始同步回填 | `parent` 開始向上淡出 | `now` 同時移回中央 | 已確認 |
| E06 | 退場中不再占位 | `parent` opacity 正由 1 降到 0 | `now` 的位置不再被 `parent` 推開 | 已確認，前提採用 A05 建議 |
| E07 | 讓位途中再次賦值 | `i` 正向左讓位時，下一個賦值事件要讓它移到另一格 | 先完整播放目前的讓位動畫；指標抵達讓位位置後，才開始下一個賦值移動，不從讓位途中的位置提前轉向 | 已確認：先完成讓位動畫後再移動。 |
| E08 | 多人進退的事件順序 | 同格有 `a,b`；若 scope exit 事件先讓 `a` 退場，下一個 declaration 事件才讓 `c` 入場，兩段動畫依序播放。若同一事件批次同時移除 `a` 並加入 `c`，則由 `a,b` 直接過渡成 `b,c` | 不同事件嚴格依序完成，先退場再入場；只有事件資料明確標示為同一批次／同一時間點時，才讓 `a` 退場與 `c` 入場交叉過渡 | 已確認：依事件順序判定；真正同時發生才交叉過渡。 |

## F. 退場

| ID | 規則 | 具體案例 | 畫面應如何處理 | 你需要確認什麼 |
|---|---|---|---|---|
| F01 | 單一指標退場 | 只有 `now → heap[4]`，`now` 離開 scope | 原地向上淡出，不先向左或向右移 | 已確認 |
| F02 | 左側指標退場 | `i` 在左、`j` 在右，`i` 退場 | `i` 向上淡出，`j` 同時回中央 | 已確認 |
| F03 | 右側指標退場 | `i` 在左、`j` 在右，`j` 退場 | `j` 向上淡出，`i` 同時回中央 | 已確認 |
| F04 | 三人其中一人退場 | `i,j,k` 同格，`j` 退場 | `j` 一開始退場就退出讓位群組；`j` 向上淡出的同時，`i,k` 立即開始移向新的兩人對稱位置。立即重排是立即開始動畫，不是瞬間跳到終點 | 已確認：使用立即重排，與 A05 統一。 |
| F05 | 同一 scope 多人退場 | 函式返回時 `now,parent,left,right` 一起失效 | 同一 exit batch 內同時退場 | 已確認 |
| F06 | 不同事件連續退場 | 先退出內層 block 的 `left`，稍後函式返回退出 `now` | 依事件順序分兩段播放 | 已確認 |
| F07 | 新舊 lifetime 不交叉 | 舊 `now` 的 scope exit 事件後，下一個 declaration 事件建立新 `now` | 先完整播放舊 `now` 退場，再播放新 `now` 入場；兩者使用不同身分，也不得交叉顯示 | 已確認：與 C05、C06 的事件順序統一。 |
| F08 | 結構縮短後索引超出範圍 | `now=8` 並指向 `heap[8]`；`pop_back` 後合法範圍縮成 `heap[1:7]`，但 `now` 仍為 8 | 這與長度為 10 卻出現索引 12 是同一種 out-of-range 狀態，不另外讓指標跟格子退場。`pop_back` 動畫進行時，同一個 `now` 從原本第 8 格的位置連續移到索引 8 對應的結構外側位置，不淡出、不重新入場，也不在動畫結束後突然跳位 | 已確認：統一視為 out-of-range／unresolved，不建立 F08 特例。 |
| F09 | resize 中退場 | heap 正在縮小，`parent` 同時退場 | 指標以當下格子幾何為基準向上淡出 | 已確認 |

## G. lifetime、函式與遞迴

| ID | 規則 | 具體案例 | 畫面應如何處理 | 你需要確認什麼 |
|---|---|---|---|---|
| G01 | 同名不同 lifetime 不延續 | 第一次 `heap_push` 的 `now` 與第二次呼叫的 `now` | 第一個退場，第二個重新入場 | 已確認 |
| G02 | 不可見 lifetime 不讓位 | 舊 `now` 已完全退場，新 `now` 獨自在格子上 | 新 `now` 置中，不保留舊位置 | 已確認 |
| G03 | 尚未退場仍讓位 | 舊 `parent` 還看得見，新 `now` 移到同格 | 兩者暫時分開 | 已確認 |
| G04 | reference parameter | `heapify(vector<int>& heap)` 內的 `heap` 與呼叫端 `heap` 是同一容器 | 容器本身保持視覺連續 | 已確認 |
| G05 | reference parameter marker | 區域 `i` 傳入 `move(int& pos)`；或全域 `i` 傳入同一函式 | 區域 `i` 在進入函式時原地改名為 `pos`，返回時改回 `i`，不重新出入場；全域 `i` 保持顯示，另建立 `pos` 別名指標，兩者同步移動 | 已確認：是否建立獨立 parameter marker 取決於原變數是否為全域變數。 |
| G06 | 遞迴 activation | quick sort 左遞迴與右遞迴都有 `i` | 每層 `i` 有獨立 lifetime，不互相污染 | 已確認 |
| G07 | 返回上一層 | 子遞迴的 `i` 退場，上層 `i` 恢復 | 上層指標回到離開前的位置與身分 | 已確認 |
| G08 | 同層角色延續 | 同一 activation 內 `left` 連續多幀未改值 | 指標保持原位，不重新入場 | 已確認 |

## H. 箭頭方向與端點

| ID | 規則 | 具體案例 | 畫面應如何處理 | 你需要確認什麼 |
|---|---|---|---|---|
| H01 | 單一指標箭頭 | `now` 單獨指向 40px 格子 | 從標籤下方垂直指向格子上緣 | 已確認 |
| H02 | 單格多人箭頭 | `i`、`j` 左右讓位後都指向同一個 40px 格子 | 所有箭頭都斜向同一個格子中心 | 已確認：目標只有一格寬時，箭頭朝向中間。 |
| H03 | 寬格箭頭 | `now` 指向寬 160px 的 heap 父節點 | 即使標籤有讓位，箭頭仍垂直向下 | 已確認 |
| H04 | 寬格多人 | `now`、`parent` 都指向寬 160px 節點 | 每支箭頭由自己的標籤垂直向下，分別指向標籤正下方的節點上緣 | 已確認：目標寬度大於一格時，箭頭保持垂直。 |
| H05 | 水平讓位中 | `now` 從中央向左讓位 | 箭頭起點跟隨標籤，終點依單格／寬格規則更新 | 已確認 |
| H06 | 跨格移動中 | `now` 從寬格移向單格，或從單格移向寬格 | 箭頭跟隨標籤移動，但移動途中沿用來源格的方向規則；抵達後才切換成目的格的方向規則 | 已確認，沿用 D10 |
| H07 | 退場中 | `parent` 向上淡出 | 箭頭與標籤一起移動並淡出 | 已確認 |
| H08 | 箭頭長度不得被動畫壓縮 | 比較動畫把格子向上抬 20px；若指標群組沒有同步抬起，原本的箭頭會縮短 20px | 格子、標籤與箭頭端點套用同一份結構位移，維持統一的標準間距；若箭頭變短，視為座標合成錯誤，不使用隱藏箭頭或臨時抬高標籤補救 | 已確認：正常狀態不會出現箭頭太短。 |
| H09 | 箭頭穿過其他標籤 | 左側 `i` 的斜箭頭在極端排列下可能接近或穿過中央 `j` | 維持統一的標籤排列與箭頭方向，不做繞線、端點偏移或額外碰撞修正；此情況幾乎不會出現 | 已確認：不處理箭頭與其他標籤的碰撞。 |
| H10 | unresolved／虛擬格箭頭 | 單一 `i=-1` 停在陣列左側時，箭頭指向索引 -1 的虛擬格中心；若 `i,j` 同時位於同一個虛擬格，兩個正方形標籤維持單排讓位 | 單一指標指向虛擬格中心；同一虛擬格有複數指標時，每支箭頭由各自標籤垂直朝下，不讓多支斜箭頭集中交會 | 已確認：指向虛擬格；複數指標時箭頭全部朝下。 |

## I. 賦值框

| ID | 規則 | 具體案例 | 畫面應如何處理 | 你需要確認什麼 |
|---|---|---|---|---|
| I01 | 框位於指標上方 | 執行 `now = parent` | 賦值框底緣位於當下 `now` 標籤頂緣上方 | 已確認 |
| I02 | 框跟隨移動 | `now = parent` 同時令 `now` 從第 6 格移到第 3 格 | 框與 `now` 沿相同 x/y 路徑移動 | 已確認 |
| I03 | 框跟隨讓位 | `now` 移入已有 `parent` 的格子 | `now` 的框跟著 `now` 最終讓位位置 | 已確認 |
| I04 | lifetime 綁定 | 舊 `now` 與新 `now` 同名 | 框只出現在事件發生時有效的舊 `now` 上 | 已確認 |
| I05 | 每個事件只有一個賦值框 | 程式依序執行 `left=now*2`、`right=now*2+1` | 先顯示並完成 `left` 的賦值框，再依下一個事件顯示 `right` 的賦值框；不把相鄰賦值合併成同時播放 | 已確認：依事件順序逐一顯示。 |
| I06 | 賦值框與其他指標的視覺重疊 | `now` 指向較低一層的 heap 節點，賦值框顯示在 `now` 標籤上方；該位置剛好已有指向上一層節點的 `parent` 標籤，兩個方框在畫面上互相蓋住 | 不做額外避讓，維持統一的指標、箭頭與賦值框位置。較下方的指標通常較晚繪製，因此具有較高繪製優先級並顯示在上層；罕見重疊時沿用此順序 | 已確認：不移動賦值框或指標，使用既有繪製順序處理視覺重疊。 |
| I07 | 賦值後同格指標立刻退場 | `now=parent` 完成後，下一個 scope exit 事件讓區域變數 `parent` 退場 | `now` 的賦值框與移動完整結束時，`parent` 仍在畫面並參與讓位；不額外停頓，接著才播放 `parent` 退場及 `now` 回填 | 已確認：沿用 D07 的事件順序。 |
| I08 | 寬格上的框 | `now` 位於寬 heap 節點且向旁邊讓位 | 框應跟著標籤，不以整個寬格中心定位 | 已確認 |

## J. heap 尺寸與 sequence

| ID | 規則 | 具體案例 | 畫面應如何處理 | 你需要確認什麼 |
|---|---|---|---|---|
| J01 | 擴張前保持舊幾何 | `push_back` 將可見節點從 3 增加到 4 | sequence 開始前，outerframe、格子、index、文字都不動 | 已確認 |
| J02 | 擴張同步 | sequence 開始增加第 4 格 | outerframe 展開，既有格與文字同步移到新位置 | 已確認 |
| J03 | 新格進場 | 第 4 格出現在 heap 右側 | 新格從外側移入並由透明變可見 | 已確認 |
| J04 | 內縮前保持舊幾何 | `pop_back` 將可見節點從 8 減為 7 | sequence 開始前所有格保持原位 | 已確認 |
| J05 | 內縮同步 | outerframe 開始縮小 | 剩餘格、index、文字與 outerframe 同時移動 | 已確認 |
| J06 | 移除格退場 | 尾格被 `pop_back` 移除 | 尾格向外移動並同步淡出 | 已確認 |
| J07 | 指標指向新增格 | `push_back` 新增尾格後，下一個事件讓 `now` 指向該格 | 先完成新格進場與 heap 擴張，再開始 `now` 的入場或移動；不得讓兩個相鄰事件的動畫重疊 | 已確認：嚴格依事件順序播放。 |
| J08 | 指標指向被移除的格子 | `now=8` 指向尾格；`pop_back` 把合法範圍由 `heap[1:8]` 改成 `heap[1:7]` | `now` 的 lifetime 不因格子移除而結束；在 heap 內縮期間連續移到索引 8 的結構外側位置，後續賦值或退場再按事件順序播放 | 已確認：沿用 F08 的一般 out-of-range 規則。 |
| J09 | resize 時重新計算全部樣式與標籤 | `now`、`parent` 同格；`push_back` 或 `pop_back` 事件正在改變 heap 寬度、outerframe 與節點中心 | resize 的每個 tick 先取得當下 heap 幾何，再重新計算所有格子 style、highlight、value/index 文字、指標標籤、同格讓位、箭頭及賦值框位置；全部與 outerframe 寬度變化同時進行，不能等 resize 結束才跳到新位置 | 已確認：heap 寬度只由 `push_back`／`pop_back` 事件改變，所有相關視覺在該事件中同步重算。 |
| J10 | index 與 highlight | heap 使用 `labels(value,index)` | 一般 highlight 包含 value＋index；compare 只包含 value | 已確認 |

## K. 比較與 highlight

| ID | 規則 | 具體案例 | 畫面應如何處理 | 你需要確認什麼 |
|---|---|---|---|---|
| K01 | 比較開始停用一般 highlight | `heap[parent] >= heap[now]` 開始抬起兩格 | 格子一開始抬起，一般 highlight 就隱藏 | 已確認 |
| K02 | compare 只框 value | heap 同時顯示 value 與 index | 比較框只包 value 格，不包 index | 已確認 |
| K03 | 比較結束恢復樣式 | compare 動畫完成 | 重新顯示目的幀應有的一般 highlight | 已確認 |
| K04 | 指標跟隨抬起格 | `now` 指向正在比較並抬起的格子 | 標籤、箭頭與格子套用相同抬起位移，整組一起移動，維持標準箭頭間距 | 已確認：指標整組跟著抬起。 |
| K05 | 多指標跟隨抬起 | `now`、`largest` 指向同一個比較格 | 兩個指標維持既有讓位排列，連同箭頭與格子一起抬起 | 已確認：整個讓位群組同步抬起。 |
| K06 | 比較後立即賦值 | `heap[left] > heap[largest]` 的比較事件後，下一個事件執行 `largest=left` | 先完整播放比較、讓格子與指標群組回到穩定位置並恢復目的幀 style；不增加額外停頓，接著才播放 `largest=left` 的賦值框與指標移動。兩個事件動畫不得重疊 | 已確認：所有事件依序播放，比較完成後才開始賦值。 |
| K07 | compare 與 resize 相鄰 | 比較結束後立即 `pop_back` | highlight 應在新幾何恢復，不留在舊位置 | 已確認 |

## L. 上一步：直接回到穩定幀

| ID | 規則 | 具體案例 | 畫面應如何處理 | 你需要確認什麼 |
|---|---|---|---|---|
| L01 | 上一步的用途 | 目前在第 38 幀，使用者按上一步回到第 37 幀，目的是稍後再按下一步重播第 37→38 幀 | 不反向播放第 38 幀的事件時間線，直接顯示第 37 幀的穩定狀態 | 已確認：上一步用於返回後重播，不需要反向動畫。 |
| L02 | 動畫中按上一步 | 第 38 幀的 heap resize 只播放到一半時按上一步 | 立即取消目前動畫及其排程，清理暫時視覺，直接建立第 37 幀穩定狀態 | 已確認：可越過事件時間線。 |
| L03 | 每次退一個 frame | 第 40 幀連按三次上一步 | 依序直接顯示第 39、38、37 幀的穩定狀態；每次操作仍準確退一個 frame | 已確認 |
| L04 | sequence 不倒播 | 第 38 幀包含 `push_back` 擴張，回第 37 幀 | 不播放 outerframe 反向收縮或新格反向淡出，直接使用第 37 幀的結構幾何與內容 | 已確認 |
| L05 | marker 不倒播 | 第 38 幀讓 `now` 從 A 移到 B，回第 37 幀 | 不播放 B→A，也不反向播放入場、退場或讓位；直接建立第 37 幀的 marker 位置與 lifetime | 已確認 |
| L06 | compare 不倒播 | 第 38 幀播放比較抬起與 compare highlight，回第 37 幀 | 不反向播放比較，直接使用第 37 幀的穩定格子與 highlight | 已確認 |
| L07 | 賦值框不倒播 | 第 38 幀曾顯示 `now=parent` 賦值框，回第 37 幀 | 不顯示反向賦值框，直接還原第 37 幀的指標文字、位置與 style | 已確認 |
| L08 | 穩定幀為唯一結果來源 | 第 38 幀同時包含 compare、assign、scope exit 與 sequence，回第 37 幀 | 不逐一反算事件；由第 37 幀資料重新建立 DOM、style、marker group 與結構幾何 | 已確認 |
| L09 | 取消動畫後完整清理 | compare 或 sequence 播放途中按上一步 | 移除 ghost、暫時 highlight、style suspension、未完成 transform、opacity 與 animation handle | 已確認 |
| L10 | 結果等同直接開啟該幀 | 從第 38 幀返回第 37 幀 | 畫面必須與直接跳到第 37 幀完全一致，不受第 38 幀已播放進度影響 | 已確認 |
| L11 | 返回後重播 | 回到第 37 幀穩定狀態，停頓後按下一步 | 正常播放第 37→38 幀的正向事件動畫，達到重播目的 | 已確認 |
| L12 | 第一幀邊界 | 已在第 0 幀時按上一步 | 保持第 0 幀穩定狀態，不建立動畫或負索引請求 | 已確認 |

## M. 快速下一步與動畫中斷

| ID | 規則 | 具體案例 | 畫面應如何處理 | 你需要確認什麼 |
|---|---|---|---|---|
| M01 | 一般單次下一步 | 第 10 幀穩定顯示一段時間後，使用者按一次下一步 | 正常播放第 10→11 幀的事件動畫 | 已確認 |
| M02 | 快轉觸發條件 | 第 10→11 幀仍在播放時，使用者再次按下一步；或前一動畫剛結束後很短時間內再次按下一步 | 需要判定何時從正常播放切換成快轉模式 | 【待填】請選擇：只要動畫未結束又收到下一步就進入快轉，或使用固定連按時間門檻。建議以「動畫未結束時再次按下一步」作為主要條件，另用短時間門檻涵蓋剛結束的情況。 |
| M03 | 每次輸入都要計數 | 從第 10 幀快速按五次下一步 | 最終目標是第 15 幀；即使略過動畫，也不能吞掉或重複計算任何一次操作 | 已確認 |
| M04 | 快轉時略過中間動畫 | 第 10→11 幀動畫中再次按下一步，目標改為第 12 幀 | 取消未完成動畫、清理暫時視覺，略過第 11、12 幀的事件時間線並直接建立目前目標幀的穩定狀態；後續連按同樣直接前進 | 已確認：快速下一步用於快轉。 |
| M05 | 快轉停止時的最後一幀 | 快速按到第 15 幀後停止操作 | 可讓第 15 幀保持已建立的穩定狀態，或補播第 14→15 幀動畫 | 【待填】建議保持第 15 幀穩定狀態，不補播動畫；下一次停頓後的下一步再恢復正常動畫。 |
| M06 | 退出快轉模式 | 使用者停止連按下一步 | 系統需要判斷何時恢復正常單步動畫 | 【待填】請決定停止輸入多久算結束快轉；建議使用與連按判定相同的時間門檻。 |
| M07 | 快轉中按上一步 | 已快速前進到第 15 幀時按上一步 | 取消快轉與所有未完成動畫，直接顯示第 14 幀穩定狀態，沿用 L 區規則 | 已確認 |
| M08 | 拖曳或點擊時間線 | 由第 10 幀直接跳到第 80 幀 | 不播放 70 個中間 frame 的事件動畫，直接顯示第 80 幀穩定狀態 | 【待填】請確認是否與上一步／快轉採用相同的穩定幀重建流程；建議相同。 |
| M09 | 中斷動畫清理 | heap resize、compare 或 marker assignment 中途進入快轉 | 在建立目標穩定幀前，清除 ghost、暫時 highlight、style suspension、未完成 transform、opacity、賦值框與 animation handle | 已確認 |
| M10 | 按住按鍵或按鈕連發 | 按住右方向鍵或下一步按鈕，瀏覽器持續產生 repeat 事件 | 每個有效 repeat 都算一次下一步，並沿用相同快轉模式 | 【待填】請確認按住連發是否與手動快速點擊完全相同；建議相同。 |
| M11 | 中途改事件開關 | 動畫播放時關閉 compare | 可立即取消並用目前 frame 的穩定狀態重建，或等下一幀才生效 | 【待填】建議立即取消並重建目前幀。 |
| M12 | 調整速度 | 動畫速度從 1x 改為 2x | resize、指標、ghost、opacity 必須共用倍率；快轉模式仍直接跳穩定幀，不受倍率影響 | 已確認 |
| M13 | 切換分頁再回來 | 動畫中瀏覽器暫停 requestAnimationFrame | 可從暫停點續播，或依實際經過時間直接完成 | 【待填】建議從暫停點續播；若期間收到導覽請求，則依上一步／快轉規則直接重建穩定幀。 |

## N. 多種效果的計算優先順序

| ID | 優先層 | 具體案例 | 建議計算方式 | 你需要確認什麼 |
|---|---|---|---|---|
| N01 | 基本格子幾何 | 目的幀中 `heap[3]` 的中心是 `(400,300)` | 先取得來源／目的幀基本位置 | 已確認 |
| N02 | resize 幾何 | heap 展開一半時 `heap[3]` 中心暫時是 `(380,280)` | 使用 resize 當下中心取代最終中心 | 已確認 |
| N03 | sequence edge 位移 | 新尾格還需從右側 48px 移入 | 在 resize 幾何上加 sequence offset | 已確認 |
| N04 | 指標跨格移動 | `now` 同時由第 6 格移到第 3 格 | 再計算 marker assignment 路徑 | 已確認 |
| N05 | 同格讓位 | `parent` 已在第 3 格，且 heap 正在 resize | 每個 tick 先取得 resize 當下的目標中心，再以通用版面函式計算同格讓位 offset；標籤位置隨兩者合成結果同步更新 | 已確認：先使用當下結構幾何，再計算讓位。 |
| N06 | 入退場垂直位移 | `parent` 同時開始向上退場 | 在水平讓位後加入 exit y offset | 已確認 |
| N07 | 賦值框 | `now=parent` 的提示框跟著 `now` | 以完成上述合成後的指標位置為錨點 | 已確認 |
| N08 | compare 抬起 | 指標所在格向上抬 20px | 在完成基本格子幾何與同格讓位後，將同一個 compare 位移套用到格子和整個指標群組，避免箭頭長度改變 | 已確認：格子與指標群組一起抬起。 |
| N09 | 單一 opacity 動畫來源 | `push_back` 事件先讓新格淡入，下一個 declaration 事件才讓指標入場；或指標退場時，父群組與指標本身都誤套 `opacity=0.5`，造成實際顯示為 `0.25` | 不同事件的透明度動畫依序播放，不做跨事件合成。每個視覺元素同一時間只能有一個 opacity 動畫來源，父群組與子元素不得重複套用同一個淡入／淡出；若同一元素收到兩個 opacity 動畫，視為排程錯誤，不以相乘作為正常行為 | 已確認：opacity 不重複合成，每個元素只有一個動畫來源。 |
| N10 | highlight layer | 格子 resize、抬起並被 highlight | highlight 跟隨最後呈現矩陣，不重複套用位移 | 已確認 |

## O. 建議優先回答的決策

先填以下項目，其餘規則大多可以由這些答案推導。

| 決策 | 對應列 | 問題 | 建議答案 | 你的答案 |
|---|---|---|---|---|
| Q01 | A07 | 同格指標的固定左右順序依什麼決定？ | 固定 `sortKey`／宣告順序 | 已確認 |
| Q02 | B02 | 兩個標籤框之間至少留多少 px？ | 8px | 已確認 |
| Q03 | B03、B04 | 三個以上指標使用單排還是多排？ | 單排向左右展開，間距沿用 8px | 已確認 |
| Q04 | H02～H04 | 多人指向同格時箭頭如何決定方向？ | 單格全部指向中心；寬度大於一格時各自垂直向下 | 已確認 |
| Q05 | C05、C06 | 不同 lifetime 的同名指標是否可能同時可見？ | 不會；舊 lifetime 完成退場後，新 lifetime 才入場 | 已確認 |
| Q06 | D03～D05 | 指標跨格移動時，來源回填與目的讓位何時開始？ | 來源在移動開始時回填；目的在指標接近時才讓位 | 已確認；「接近」的精確觸發點待定義 |
| Q07 | F08、J08 | 指標指向的格子被移除時怎麼辦？ | 指標仍存在並連續移到相同索引的結構外側位置，視為一般 out-of-range | 已確認 |
| Q08 | H08、K04、K05、N08 | 比較格抬起時，指標是否一起抬起？ | 指標群組一起抬起，維持統一箭頭間距 | 已確認 |
| Q09 | L01～L08 | 上一步是否倒播任何事件？ | 不倒播，直接建立前一幀穩定狀態 | 已確認 |
| Q10 | L09～L11 | 動畫途中按上一步如何處理？ | 取消並完整清理目前動畫，直接建立前一幀穩定狀態；下一次正常下一步可重播 | 已確認 |
| Q11 | M03、M04 | 快速連按下一步時是否播放中間事件動畫？ | 不播放；每次輸入照常計數並直接建立目前目標幀穩定狀態 | 已確認 |
| Q12 | B06 | 相鄰格子的標籤碰撞是否納入避讓？ | 暫不處理，只處理同格讓位 | 已確認 |
| Q13 | A08、G05 | runtime alias／reference parameter 是否顯示獨立指標？ | 區域變數原地改名；全域變數保留原名並另顯示 parameter | 已確認 |
| Q14 | M08 | 拖曳或點擊時間線跳多幀時是否播放中間動畫？ | 不播放，直接顯示目標穩定狀態 | 【待填】 |
| Q15 | M02 | 什麼情況開始快速下一步模式？ | 動畫未結束又收到下一步；另用短時間門檻涵蓋剛結束的情況 | 【待填】 |
| Q16 | M05、M06 | 停止連按後，最後一幀是否補播動畫，多久恢復正常模式？ | 最後一幀維持穩定狀態，不補播；超過連按時間門檻後恢復正常 | 【待填】 |
| Q17 | M10 | 按住按鍵或按鈕產生的 repeat 是否等同快速點擊？ | 是，每個有效 repeat 前進一幀 | 【待填】 |
