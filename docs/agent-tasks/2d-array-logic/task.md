# 二維陣列第一版

- 分支：`codex/2026-09-24-beta-2d-array-logic`
- 基底：`intergration`
- 不延伸 `range` 語法。
- 實作 ragged matrix、row/column/inner labels、共用自訂標籤來源、`labels(none)`、`gridlines(width)`、`outerframe(bool)`、`grid[i][j]` 軸向指標。
- 固定資料格 40px；inner label 高 12px。
- 一般 cell highlight 不包含 row/column label，但包含該 cell 的 inner label。

