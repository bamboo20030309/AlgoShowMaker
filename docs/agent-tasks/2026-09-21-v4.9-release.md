# v4.9 main 合併與發布紀錄

- 日期：2026-09-21。
- 使用者要求：將已驗證整合版本合併 main，並發布新 release。
- 整合來源：`intergration` 的 `d06681c197fb6512a88ca1333065b7477147ec1e`。
- main 合併 commit：`897dd7e`。
- 發布說明 commit：`31dea6b`。
- Release：`v4.9`，標籤指向包含本紀錄的最終 main 發布提交。
- 發布說明：`docs/releases/v4.9.md`；版本日誌：`version_log.txt`。

## 驗證

- 合併前聯合集合 16 檔、123/123 通過。
- Heap 限定動畫回歸：algorithm 548、editor 543、runtime 548 samples 通過；cloud storage、slide order、deck import repair 通過。
- main 合併後重跑 entrypoints、event defaults、provenance，12/12 通過；三個主要 JavaScript 語法及 `git diff --check` 通過。
- main 與已驗證 integration 的程式差異為零；只新增 v4.9 發布說明與版本日誌。
- main 3000 已從本專案根工作目錄啟動為 PID 4084；algorithm、slides、`/trace/analyze` 均為 HTTP 200。
- 前端版本核對：tween `trace-222`、renderer `trace-202`、Studio `trace-121`、front `random-id-34`。

## 發布範圍

- 推送 `main` 與 `v4.9` tag 至 origin。
- 建立 GitHub Release `v4.9`，正文使用 `docs/releases/v4.9.md`。
- 本次未操作公開主機部署或正式資料庫。
