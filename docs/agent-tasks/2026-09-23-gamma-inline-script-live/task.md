# 2026-09-23-gamma-inline-script-live：文字上下標即時格式化

## 任務資訊
- 負責代理：gamma
- 狀態：待交付
- 共同基準 commit：2ddbb6dffd5b894358c3329ecb4a97c88289185a
- 分支：codex/2026-09-22-gamma
- Worktree：C:\Users\user\Documents\Codex\2026-07-29\algoshowmaker-main-commit-d154dd5-slides-html\work\AlgoShowMaker\.worktrees\2026-09-22-gamma

## 問題與預期結果
- 情境與操作：在一般文字物件啟用上下標解析後輸入 `1^n`。
- 目前行為：編輯期間不套用上標，離開編輯後也可能沒有套用。
- 使用者希望的結果：完成 `^n` 輸入時立即顯示上標，離開編輯、復原及儲存時仍維持正確。
- 本次範圍與必要限制：一般 Fabric 文字物件的 `_`、`^` 行內格式；不新增根號，不執行演算法驗證集。

## 需求確認
- 已從使用者或上下文確認：上下標應在打完語法時觸發；優先沿用既有文字元件與行內格式器。
- 尚待使用者回答：無。
- 代理採用的合理假設：單字元語法在目標字元輸入後套用；不完整的 `^` 或 `_` 保持普通文字。

## 重現與調查
- 最小操作步驟或 fixture：匯入已啟用 `asmInlineScripts` 的文字物件，進入編輯並輸入 `1^n`，檢查編輯中及離開後的字元樣式。
- 重現狀態：已從事件流程確認。
- 已確認事實：`applyInlineScripts` 在 `obj.isEditing` 時直接返回；離開編輯事件發生時仍可能處於該狀態。
- 尚待調查：即時格式套用後連續輸入、復原與樣式基準是否保持正確。

## 修改邊界與依賴
- 預計修改檔案或模組：`algo-vis-backend/public/slides.js`、相關文字瀏覽器測試、任務文件。
- 共用檔案／介面與協調結果：沿用 `ASMInlineScripts.format` 與現有 Fabric 自訂欄位，不更改 deck 格式。
- 依賴任務：無。

## 驗收條件
- [x] 已啟用上下標解析的文字物件輸入 `1^n` 後，不離開編輯即可看到 `n` 為上標且 `^` 隱藏。
- [x] 繼續輸入、離開編輯及重新進入時，原始文字與格式保持正確。
- [x] 文字復原仍能還原文字、游標與上下標格式。

## 驗證計畫
- 子代理小驗證：JS 語法檢查、行內格式單元測試、相關 Fabric 文字瀏覽器測試。
- 主代理整合驗收：在整合版本實際輸入 `1^n`、復原並重開投影片。
- 測試隔離方式：測試檔自行啟動隨機埠與獨立 headless Edge，使用臨時匯入 deck。

## 變更紀錄
- 2026-09-23：依使用者回報建立任務；要求上下標完成輸入時立即觸發。
