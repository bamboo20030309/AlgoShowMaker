# 2026-09-24-gamma-text-edit-performance：文字操作與復原效能

## 任務資訊
- 負責代理：gamma
- 狀態：待交付
- 共同基準 commit：2b76e589f63706684ae24bf22ba74b86ad3ade92
- 分支：codex/2026-09-22-gamma
- Worktree：C:\Users\user\Documents\Codex\2026-07-29\algoshowmaker-main-commit-d154dd5-slides-html\work\AlgoShowMaker\.worktrees\2026-09-22-gamma

## 問題與預期結果
- 情境與操作：一般文字物件較多或整份投影片較大時，輸入、拖曳及復原感覺慢半拍。
- 目前行為：每個字都重設當頁所有文字樣式、序列化整份 deck、排入 IndexedDB 並建立完整歷史快照；文字復原解析及比較完整 deck；文字拖曳每幀不使用 Fabric 快取。
- 使用者希望的結果：依實測診斷修正上述熱點，文字輸入、復原及拖曳更即時，同時保留資料與格式正確性。
- 本次範圍與必要限制：一般 Fabric 文字編輯、undo/redo、本機草稿排程及拖曳重繪；不修改動畫與公開資料格式，不執行大型 regression。

## 需求確認
- 已從使用者或上下文確認：使用者同意先前提出的四項修正方向；完成後需 commit 並 push。
- 尚待使用者回答：無。
- 代理採用的合理假設：連續輸入以約 300 ms 閒置作為一個輕量復原節點；離開文字編輯時建立完整 deck 歷史點。

## 重現與調查
- 最小操作步驟或 fixture：分別使用少量文字、同頁 60 個文字物件及約 1 MB deck，逐字輸入並量測輸入、儲存、復原與重繪。
- 重現狀態：已重現。
- 已確認事實：60 個文字物件每鍵的 60 次 `set('styles')` 約占 36.9 ms；1 MB deck 復原會執行多次完整解析與序列化；關閉文字快取的重繪約為啟用時的 9 倍。
- 尚待調查：修正後量測與既有 IME、上下標、特殊字元行為回歸。

## 修改邊界與依賴
- 預計修改檔案或模組：`algo-vis-backend/public/slides.js`、相關文字瀏覽器測試、任務文件。
- 共用檔案／介面與協調結果：deck 與 asmdeck 格式不變；完整歷史仍使用既有格式，僅編輯期間新增記憶體內輕量狀態。
- 依賴任務：延續 `2026-09-23-gamma-inline-script-live` 的即時上下標事件流程。

## 驗收條件
- [x] 未變更的文字物件不會在每次輸入時重設 `styles`。
- [x] 快速連續輸入只排程一次本機草稿寫入，且離開編輯後資料可重載。
- [x] 編輯期間 undo/redo 只還原目前文字物件，保留游標、樣式、同層其他物件與即時上下標。
- [x] 文字物件拖曳／縮放期間使用快取，操作結束後恢復高解析設定並重繪。
- [x] 修正後大型文字頁面的輸入與復原量測明顯低於修正前基準。

## 驗證計畫
- 子代理小驗證：JS 語法與差異檢查；文字 undo、特殊字元與上下標瀏覽器測試；新增效能行為斷言及隔離量測。
- 主代理整合驗收：大型文字頁面實際輸入、拖曳與 undo/redo，確認手感及重載資料。
- 測試隔離方式：瀏覽器測試自行啟動隨機埠、headless Edge 與臨時匯入 deck，不操作使用者分頁。

## 變更紀錄
- 2026-09-24：依使用者同意的效能建議建立任務。
