# 2026-09-26-gamma-editor-slide-delete：編輯器直接刪除投影片頁面

## 任務資訊
- 負責代理：gamma
- 狀態：待交付
- 共同基準 commit：a87a8321f96926a271cbb71ae2112ba2a34503c0
- 分支：codex/2026-09-22-gamma
- Worktree：C:\Users\user\Documents\Codex\2026-07-29\algoshowmaker-main-commit-d154dd5-slides-html\work\AlgoShowMaker\.worktrees\2026-09-22-gamma

## 問題與預期結果
- 情境與操作：在投影片編輯器或投影片順序總覽中刪除一張投影片頁面。
- 目前行為：刪除前會顯示自訂確認視窗，需要再次確認。
- 使用者希望的結果：編輯器內按下刪除後立即刪除頁面；首頁刪除整份投影片仍保留確認流程。
- 本次範圍與必要限制：只調整編輯器內的頁面刪除流程；保留刪除動畫、復原能力與至少一張頁面的限制；不執行大規模驗證。

## 需求確認
- 已從使用者或上下文確認：首頁刪除需要確認；編輯器內刪除不需要確認；完成後需 commit 並 push。
- 尚待使用者回答：無。
- 代理採用的合理假設：編輯器總覽中的單張與多張刪除都屬於編輯投影片，應使用相同的直接刪除行為。

## 重現與調查
- 最小操作步驟或 fixture：開啟含兩張頁面的 deck，進入編輯模式與投影片總覽，選取頁面後按 Delete。
- 重現狀態：已重現。
- 已確認事實：`deleteOverviewSelectedSlide` 會先呼叫 `requestSlideDeleteConfirmation`；首頁整份 deck 的刪除由 `home.js` 的獨立事件處理。
- 尚待調查：無。

## 修改邊界與依賴
- 預計修改檔案或模組：`algo-vis-backend/public/slides.js`、`slides.html`、`slides.css`、局部瀏覽器測試與入口版本測試。
- 共用檔案／介面與協調結果：不修改 deck 格式、首頁刪除事件或儲存介面。
- 依賴任務：無。

## 驗收條件
- [x] 編輯器內選取頁面並按 Delete 後，不顯示確認視窗且立即開始刪除。
- [x] 刪除後可使用復原恢復頁面，且最後一張頁面仍不可刪除。
- [x] 首頁刪除整份投影片的確認流程保持原狀。

## 驗證計畫
- 子代理小驗證：JavaScript 語法、差異檢查、入口載入測試，以及使用隨機埠與 headless Edge 驗證兩張頁面的直接刪除與復原。
- 主代理整合驗收：實際操作首頁整份 deck 刪除確認，以及編輯器頁面單張／多張直接刪除與復原。
- 測試隔離方式：瀏覽器測試自行啟動隨機埠並注入臨時 localStorage deck，不操作使用者分頁或既有投影片。

## 變更紀錄
- 2026-09-26：依使用者指示，將刪除確認限定在首頁整份投影片刪除；編輯器頁面刪除恢復為直接執行。
