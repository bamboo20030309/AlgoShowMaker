# 2026-09-22 整合交付紀錄

## 整合範圍

- 基準：`main` v4.9（`2cb0409`）。
- Alpha：標準線段樹 renderer、範例、格式與間距支援。
- Beta：BIT 建置／區間查詢、前綴和範例、`@let`、位元運算與迴圈初值。
- Gamma：分頁切換保留鏡頭、語法樹縮放、移除註標形狀介面。
- BIT 區間查詢移除 C++ `bool deduct` 與兩次賦值，改用 `@let deduct = start == L - 1`。
- `@let` 接受比較與邏輯運算式，使布林別名可直接用於 `when`。

## 驗證分級與選擇

- 層級：主代理 V3 限定範圍驗證。
- 分類：A、D、E、F、H、J。
- 選擇依據：此次同時影響指令解析、runtime、renderer、動畫、鏡頭與 Studio。
- 隔離服務：`http://127.0.0.1:32120`，未使用 3000／3100～3103。
- 第一輪結果：35 通過、2 失敗；失敗原因為 `@let` 尚未接受比較式。
- 修正後局部整合：37 通過、0 失敗、0 跳過。
- 瀏覽器專項：7 通過、0 失敗、0 跳過；涵蓋 BIT 綠／紅前綴、標準線段樹、二元加法舊投影片，以及分頁鏡頭／語法樹縮放。
- 語法與差異：相關 JavaScript `node --check` 與 `git diff --check` 通過。
- 未執行：未執行完整 `npm run regression`；限定整合範圍已有解析、編譯、輸出與實際瀏覽器驗證。
