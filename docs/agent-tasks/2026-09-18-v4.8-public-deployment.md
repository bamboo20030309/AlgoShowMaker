# v4.8 公開範例與部署核實

- 使用者明確要求公開六份Downloads內asmdeck、merge main、重啟219.70.89.39專案服務並建立新release。
- 範例程式提交1dfabacad98c71aa0be1188de0bf93410b710543；main合併2748c7656e0ea545fe92df5b8465d11db0145278，已push。後續僅新增本文件，不改受測程式。
- 六檔快速／冒泡／選擇／插入／堆積排序歸Sorting，線篩歸Math；保留linear-sieve ID並更新檔案。各18張、2張algorithm-animation，共108張及12張動畫。
- 原檔複製不改內容；ASMDeck.decode沿用trace-provenance引擎4/1，逐檔核實壓縮格式、版本、內容與素材SHA-256。最初Node驗證缺少provenance上下文，載入既有模組後通過，不繞過版本檢查。

## 驗證

- 隔離服務52283，ASM_REGRESSION=1、獨立JWT、不連正式Mongo；node --test tests/asmdeck.test.js tests/entrypoints.test.js test-results/public-samples.browser.cjs，共13pass/0fail/0skip，exit0，約25秒。
- 補強browser斷言：每份sample頁18張、標題正確、shared-view-only、無pageerror，以及2個runtime iframe都有實際trace.frames；隔離埠52981重跑browser，1pass/0fail/0skip，約26秒。
- 真正公開Docker部署再用ASM_TEST_BASE_URL=http://219.70.89.39執行同份browser，1pass/0fail/0skip，exit0，約48秒；六份／12個動畫iframe均成功，不以本機Windows結果代替公開Linux驗收。
- 瀏覽器獨立Edge context與公開唯讀sample，沒有操作使用者分頁或寫入私人deck。
- 本次不重跑所有演算法／所有幀／速度／聲音品質；未驗證真實帳號跨装置儲存與歷史間歇排序問題。歷次整合按範圍的專項結果見各delivery，不能宣稱所有動畫全面通過。
- 語法與差異檢查通過；本機runner/browser/TAP/log在test-results，不提交。沒有新增低影響的永久鏡像測試。

## 實際主機與服務

- 使用者提供AV_V3路徑；檢查發現AV_V3為乾淨main 948e831，但實際backend／nginx容器bind mount及working_dir指向Codex專案main。依實際容器來源更新，不修改不在掛載中的AV_V3舊checkout。
- 原Docker backend Mongo hostname為mongo、網路algo-vis-backend_default、PORT3000；沿用既有compose設定。
- 在main/algo-vis-backend執行docker compose -f docker-compose.yml build backend；up -d --no-deps backend；restart nginx。各exit0；新image sha256:0cbb34c51ec6dc9d6fe619d7cf15a3c285f2cd4c6a24bcf46d8b5047920d7020。
- backend重建／替換，nginx重啟；Mongo容器未重啟、資料卷未刪除／遷移。
- 219.70.89.39的guest-decks.json、algorithm.html、slides.html、trace-renderer.js及六份archive HTTP200，SHA-256與受測main檔一致。
- 本機3000重啟PID25872，3100重啟PID39280，HTTP200；其他代理服務未停止。
- 公開範例：http://219.70.89.39/?examples=1；發布說明docs/releases/v4.8.md，GitHub新release預計v4.8，最終URL／SHA以主代理發布核對結果為準。沒有移動舊v4.7標籤。
