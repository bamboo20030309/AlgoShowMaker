# 2026-09-23-gamma-av-palette：投影片選色器加入 AV 常用色

## 任務資訊
- 負責代理：gamma
- 狀態：待交付
- 共同基準 commit：819da7c7ff003dd58f20204b539067f072aa8e1c
- 分支：codex/2026-09-22-gamma
- Worktree：C:\Users\user\Documents\Codex\2026-07-29\algoshowmaker-main-commit-d154dd5-slides-html\work\AlgoShowMaker\.worktrees\2026-09-22-gamma

## 問題與預期結果
- 情境與操作：在投影片編輯器打開文字、圖形或 Structure 的選色器。
- 目前行為：只能手動在 iro 選色區調整，無法直接選用專案常見的 `AV_` 命名色。
- 使用者希望的結果：選色器下方顯示 `AV_` 色票，點一下就套用到目前選色目標。
- 本次範圍與必要限制：沿用現有選色器及儲存流程；純前端 V1 局部驗證，不跑大規模演算法驗證。

## 需求確認
- 已從使用者或上下文確認：在投影片選色器底下加入常用 `AV_` 色，供直接點選。
- 尚待使用者回答：無
- 代理採用的合理假設：使用既有 `ASMArrowModel.COLORS` 的全部 12 個 `AV_` 顏色作為同一組色票，保留原本的透明度數值。

## 重現與調查
- 最小操作步驟或 fixture：選取 Structure 陣列並打開 Highlight 選色器，檢查色票，再點選半透明與不透明色。
- 重現狀態：功能新增不適用。
- 已確認事實：投影片各選色按鈕共用 `#iroPopup` 與 `openIro`；現有 `color:change` 已處理套用與儲存。專案的 `trace-arrow-model.js` 匯出 12 個 `AV_` 色值。
- 尚待調查：無

## 修改邊界與依賴
- 預計修改檔案或模組：`algo-vis-backend/public/slides.html`、`slides.css`、`slides.js`、相關入口及瀏覽器測試、本任務文件。
- 共用檔案／介面與協調結果：加入既有 `trace-arrow-model.js` 作為投影片頁面的色票來源；不修改顏色資料格式。
- 依賴任務：無

## 驗收條件
- [ ] 投影片選色器下方列出既有 12 個 `AV_` 色票，名稱與視覺色塊可辨認。
- [ ] 點選半透明及不透明色票，會套用並儲存到目前選色目標；重開後保留。
- [ ] 原本的 iro 手動選色仍可使用。

## 驗證計畫
- 子代理小驗證：專項色票瀏覽器測試、既有 Structure 選色測試、入口測試、語法與差異檢查、畫面截圖檢視。
- 主代理整合驗收：在整合版檢查文字、圖形及 Structure 的共用選色彈窗與儲存。
- 測試隔離方式：專項測試用隨機埠、獨立瀏覽器 page 與內建 deck，不操作使用者資料；截圖留在本機忽略的 `test-results`。

## 變更紀錄
- 2026-09-23：依使用者要求加入既有 `AV_` 命名色，色值直接取自共用模型。
