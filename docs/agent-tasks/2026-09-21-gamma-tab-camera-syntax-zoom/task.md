# 2026-09-21-gamma-tab-camera-syntax-zoom：保留畫布鏡頭並解除語法樹縮放限制

## 任務資訊
- 負責代理：gamma
- 狀態：待交付
- 共同基準 commit：4145c160aa268ea20988da088efa4e1f1b6e0567
- 分支：codex/2026-09-21-gamma
- Worktree：C:\Users\user\Documents\Codex\2026-07-29\algoshowmaker-main-commit-d154dd5-slides-html\work\AlgoShowMaker\.worktrees\2026-09-21-gamma

## 問題與預期結果
- 情境與操作：從畫布切換到輸入、輸出、除錯紀錄或語法樹，再切回畫布；在語法樹使用滾輪縮放。
- 目前行為：返回畫布會重新執行自動鏡頭，造成原本鏡頭位置偏移；語法樹只能在整棵樹與原寬度 3.5% 之間縮放。
- 使用者希望的結果：分頁往返後維持原畫布鏡頭中心與倍率；語法樹不設介面縮放上下限。
- 本次範圍與必要限制：分頁切換鏡頭狀態與語法樹 viewBox 滾輪縮放；不跑完整 regression。

## 需求確認
- 已從使用者或上下文確認：四個非畫布分頁返回後都需維持鏡頭；語法樹需可持續縮小或放大。
- 尚待使用者回答：無。
- 代理採用的合理假設：「無限」指移除產品設定的上下限；仍保留 JavaScript 有限數值檢查，避免 viewBox 變成無效值。

## 重現與調查
- 最小操作步驟或 fixture：設定非預設畫布中心與倍率，逐一切換四個分頁再返回；記錄前後 `getCameraViewport()`。建立三節點語法樹，將 viewBox 縮放超過原上下限。
- 重現狀態：已重現。
- 已確認事實：返回畫布時固定呼叫 `setAutoCamera(1.0, false)`，直接覆寫使用者鏡頭。語法樹使用 `Math.min(baseView.width, Math.max(minimumWidth, ...))` 限制縮放。
- 尚待調查：無。

## 修改邊界與依賴
- 預計修改檔案或模組：`public/front.js`、`public/syntax-tree.js`、`public/algorithm.html`、入口與聚焦瀏覽器測試。
- 共用檔案／介面與協調結果：沿用既有 `getCameraViewport()`／`setCamera()`，不改鏡頭資料格式。
- 依賴任務：無。

## 驗收條件
- [x] 從輸入返回畫布後，中心與倍率維持不變。
- [x] 從輸出返回畫布後，中心與倍率維持不變。
- [x] 從除錯紀錄返回畫布後，中心與倍率維持不變。
- [x] 從語法樹返回畫布後，中心與倍率維持不變。
- [x] 語法樹可縮小超過整棵樹視圖，也可放大超過舊 3.5% viewBox 下限。

## 驗證計畫
- 子代理小驗證：JavaScript 語法、入口測試、獨立瀏覽器分頁鏡頭與語法樹縮放測試、diff 檢查。
- 主代理整合驗收：使用實際動畫畫布調整鏡頭後逐一切換分頁；在大型語法樹連續縮放與拖曳。
- 測試隔離方式：瀏覽器測試使用隨機本機埠、獨立 headless 瀏覽器及攔截的語法樹 fixture。

## 變更紀錄
- 2026-09-21：依使用者回報建立任務；改為保存及恢復鏡頭，並移除語法樹產品縮放限制。
