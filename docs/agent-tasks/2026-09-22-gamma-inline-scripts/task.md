# 2026-09-22-gamma-inline-scripts：一般文字上標與下標

## 任務資訊
- 負責代理：gamma
- 狀態：待交付
- 共同基準 commit：3706489c0a82b06eb0235d81057790a9578a45a0
- 分支：codex/2026-09-22-gamma
- Worktree：C:\Users\user\Documents\Codex\2026-07-29\algoshowmaker-main-commit-d154dd5-slides-html\work\AlgoShowMaker\.worktrees\2026-09-22-gamma

## 問題與預期結果
- 情境與操作：在一般 Fabric 文字物件輸入 `A_2`、`A^2` 等簡易數學語法。
- 目前行為：底線與插入符號當作普通字元顯示。
- 使用者希望的結果：字元在觀賞狀態呈現上標或下標；根號本次不做。
- 本次範圍與必要限制：新文字物件預設啟用，既有物件維持原狀但可透過文字工具列開關啟用；編輯時原始語法可見，保存、匯出、縮圖與載入保持一致。

## 需求確認
- 已從使用者或上下文確認：只做上標、下標；既有文字保持原樣、新文字預設啟用的做法已在上一輪方案提出，使用者僅排除根號。
- 尚待使用者回答：無。
- 代理採用的合理假設：採 LaTeX 的單字元腳本規則，複數字元使用大括號；`\_` 與 `\^` 保留字面符號。

## 重現與調查
- 最小操作步驟或 fixture：新增一般文字物件後輸入 `A_2` 與 `A^2`，離開編輯並重新載入。
- 重現狀態：已確認現有文字物件沒有解析流程。
- 已確認事實：一般文字由 Fabric Textbox 繪製；Fabric 5.3 已提供字元級上／下標尺寸與基線位移；縮圖從儲存的 Fabric JSON 重建。
- 尚待調查：無；已用專項瀏覽器測試及既有文字 undo 測試確認。

## 修改邊界與依賴
- 預計修改檔案或模組：`public/slides.html`、`slides.css`、`slides.js`、新解析器 `slide-inline-scripts.js`、直接相關測試。
- 共用檔案／介面與協調結果：新增 `asmInlineScripts` 和 `asmInlineScriptBaseStyles` 文字物件欄位；不修改 LaTeX 元件或根號。
- 依賴任務：無。

## 驗收條件
- [x] 新文字物件顯示 `A_2` 底標、`A^2` 上標，`x_i^2` 同時顯示兩者，括號包覆可作用於多字元。
- [x] 編輯時原始語法可見，離開編輯後恢復排版；跳脫符號保持字面顯示。
- [x] 舊文字物件預設不變，可使用文字工具列開關啟用或關閉解析。
- [x] 儲存後重開、縮圖與匯出保留正確文字及樣式，undo/redo 不丟失內容。

## 驗證計畫
- 子代理小驗證：解析器單元測試、相關投影片局部瀏覽器測試、JS 語法與 diff 檢查。
- 主代理整合驗收：整合後實際編輯與重新開啟一般文字投影片，觀察上下標、縮圖與既有格式。
- 測試隔離方式：隨機本機埠、獨立瀏覽器與本地虛構投影片，不操作使用者資料。

## 變更紀錄
- 2026-09-22：依使用者指示將原構想縮至上標與下標，根號留待未來任務。
