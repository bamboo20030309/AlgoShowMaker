# 2026-09-23-gamma-special-text-cursor：特殊文字游標定位

## 任務資訊
- 負責代理：gamma
- 狀態：開發中
- 共同基準 commit：bd9a4af388431ab68fb4538e6ecac2fecd179369
- 分支：codex/2026-09-22-gamma
- Worktree：C:\Users\user\Documents\Codex\2026-07-29\algoshowmaker-main-commit-d154dd5-slides-html\work\AlgoShowMaker\.worktrees\2026-09-22-gamma

## 問題與預期結果
- 情境與操作：在一般文字物件中移動或點擊游標，內容包含 `②`、組合字或多碼位 emoji。
- 目前行為：Fabric 5 僅將代理對視為一個字元；組合字與 ZWJ 序列會被拆成多個游標位置，後續座標及樣式索引偏移。
- 使用者希望的結果：游標按畫面看到的字素移動；點擊 `②` 前後能落在正確位置。
- 本次範圍與必要限制：一般投影片文字的字素切分、舊樣式索引遷移及行內上下標索引；不執行大規模驗證。

## 需求確認
- 已從使用者或上下文確認：`②` 是可重現代表；只需相關局部驗證；完成後需 push。
- 尚待使用者回答：無
- 代理採用的合理假設：同類特殊文字包含組合字及 ZWJ emoji，應以 Unicode grapheme cluster 作為一個可見字元。

## 重現與調查
- 最小操作步驟或 fixture：匯入含 `A②B 👨‍👩‍👧‍👦 é A_2` 的文字物件，點擊 `②` 左右並逐字移動游標。
- 重現狀態：已重現
- 已確認事實：既有 Fabric splitter 將 family emoji 拆為 7 個位置、`é` 拆為 2 個位置，造成後續游標與樣式索引偏移；`②` 為單一碼位但需納入點擊邊界回歸。
- 尚待調查：無

## 修改邊界與依賴
- 預計修改檔案或模組：`public/slides.js`、`public/slide-inline-scripts.js`、`public/slides.html`、相關專項測試。
- 共用檔案／介面與協調結果：新增序列化欄位 `asmGraphemeVersion`，舊文字樣式載入時遷移到 Unicode 字素索引。
- 依賴任務：無

## 驗收條件
- [ ] 點擊 `②` 左半與右半時，游標分別位於字元前後。
- [ ] 組合字與 ZWJ emoji 只產生一個游標位置，後方既有樣式及上下標仍套用到正確字元。
- [ ] 舊投影片文字可載入並儲存新的字素版本資訊。

## 驗證計畫
- 子代理小驗證：JS 語法、特殊文字游標 Playwright 專項、既有行內上下標專項、入口版本測試與 diff check。
- 主代理整合驗收：核實特殊文字點擊及左右鍵移動；不需演算法動畫回歸。
- 測試隔離方式：由測試啟動隨機埠與獨立無頭瀏覽器，使用臨時匯入 deck。

## 變更紀錄
- 2026-09-23：依使用者回報建立 Unicode 字素游標修正範圍。
