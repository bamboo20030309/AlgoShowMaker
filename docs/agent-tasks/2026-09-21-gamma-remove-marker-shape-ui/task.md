# 2026-09-21-gamma-remove-marker-shape-ui：移除舊註標形狀控制介面

## 任務資訊
- 負責代理：gamma
- 狀態：待交付
- 共同基準 commit：2cb0409d1fd430d1fabea1feb5885e908da02236
- 分支：codex/2026-09-21-gamma
- Worktree：C:\Users\user\Documents\Codex\2026-07-29\algoshowmaker-main-commit-d154dd5-slides-html\work\AlgoShowMaker\.worktrees\2026-09-21-gamma

## 問題與預期結果
- 情境與操作：在 Trace Studio 右側「物件」面板選取有位置綁定的索引註標。
- 目前行為：面板仍顯示舊「註標形狀」區塊，可在原始陣列與箭頭註標之間切換。
- 使用者希望的結果：刪除這個已不再需要的控制介面，並盤點其他可能過時的介面供後續判斷。
- 本次範圍與必要限制：移除控制介面及其專用程式碼與樣式；保留舊 deck 的 `markerShape` 資料讀取與播放相容性。

## 需求確認
- 已從使用者或上下文確認：舊註標形狀控制介面可刪除。
- 尚待使用者回答：其他候選舊介面是否刪除，待本次盤點後由使用者決定。
- 代理採用的合理假設：本次不移除 `markerShape` 資料欄位，以免舊投影片的既有箭頭外觀改變。

## 重現與調查
- 最小操作步驟或 fixture：開啟 Trace Studio，選取可定位到陣列格子的變數，查看右側物件面板。
- 重現狀態：已重現。
- 已確認事實：`trace-studio.js` 動態建立「註標形狀」區塊，並有 104 行專用 CSS；控制函式只由此區塊使用。`markerShape` 仍被載入及同步函式使用，需保留相容性。
- 尚待調查：無。

## 修改邊界與依賴
- 預計修改檔案或模組：`public/trace-studio.js`、`public/trace-studio.css`、`public/algorithm.html`、`tests/entrypoints.test.js`。
- 共用檔案／介面與協調結果：不改 trace/deck 資料格式，不改既有標記呈現 runtime。
- 依賴任務：無。

## 驗收條件
- [x] Trace Studio 物件面板不再建立「註標形狀」區塊。
- [x] 專用控制函式、狀態與 CSS 均已移除。
- [x] 舊 deck 的 `markerShape` 仍可被讀取並交給標記呈現邏輯。
- [x] 提供其他候選舊介面的用途與移除影響清單。

## 驗證計畫
- 子代理小驗證：JavaScript 語法、入口與介面移除斷言、標記相容性聚焦測試、diff 檢查。
- 主代理整合驗收：開啟 Trace Studio 選取物件，確認舊區塊消失；載入含箭頭 markerShape 的舊 deck，確認呈現不變。
- 測試隔離方式：只執行聚焦 Node 測試，不跑完整 regression 或全部測試。

## 待使用者判斷的舊介面
- 頂端「事件設定」：設定事件類型的全域預設動畫與時間線顯示；與 Trace Studio 的逐指令事件控制有部分重疊，但仍負責新事件的預設值。
- 演算法畫布繪圖工具列：畫筆、橡皮擦、矩形、圓形、箭頭、文字與舊式浮動選色器；若標註工作都移到投影片編輯器，可考慮移除或收進進階功能。
- 「除錯記錄」分頁：顯示程式執行／追蹤記錄，偏開發診斷用途。
- 「語法樹」分頁：顯示 AST，偏解析器開發與除錯用途。
- Trace Studio「此物件已建立效果」：彙總並可刪除規則、追蹤物件、箭頭、鏡頭與幀間動畫；不是單純文字摘要，移除前需另留刪除入口。
- Trace Studio「幀間動畫」：手動指定上一幀物件 ID 與是否補間；相同 ID 已能自動補間，只在跨 ID 或停用動畫時使用。

## 變更紀錄
- 2026-09-21：依使用者要求移除舊註標形狀控制介面，保留資料相容層並完成舊介面盤點。
