# 2026-09-23-gamma-sample-purpose-hints：範例投影片用途提示

## 任務資訊
- 負責代理：gamma
- 狀態：待交付
- 共同基準 commit：4bdf1af90ecaafb097f3d8788b33503c170c53ee
- 分支：codex/2026-09-22-gamma
- Worktree：C:\Users\user\Documents\Codex\2026-07-29\algoshowmaker-main-commit-d154dd5-slides-html\work\AlgoShowMaker\.worktrees\2026-09-22-gamma

## 問題與預期結果
- 情境與操作：訪客瀏覽首頁的範例投影片卡片。
- 目前行為：卡片只顯示演算法名稱與大分類，無法快速理解用途。
- 使用者希望的結果：每個範例在標題下顯示分類與用途提示；線篩顯示「數學、質數篩」，線段樹顯示「資料結構、區間修改、區間和」。
- 本次範圍與必要限制：為目前九個公開範例補齊簡短提示；提示由 catalog 資料驅動，不寫死在渲染程式。

## 需求確認
- 已從使用者或上下文確認：用途提示位於各範例投影片卡片下方；指定線篩與線段樹文字；完成後 push；不跑大規模驗證。
- 尚待使用者回答：無。
- 代理採用的合理假設：其他範例依其主要操作與用途補上 1–2 個簡短詞；分類與用途以小標籤呈現，並納入搜尋文字。

## 重現與調查
- 最小操作步驟或 fixture：開啟 `/?examples=1`，查看任一範例卡片標題下方。
- 重現狀態：已重現。
- 已確認事實：公開範例由 `guest-decks.json` 提供資料，`guest-gallery.js` 負責卡片與搜尋。
- 尚待調查：無。

## 修改邊界與依賴
- 預計修改檔案或模組：`guest-decks.json`、`guest-gallery.js`、`home.css`、`index.html`、入口測試與用途提示 browser test。
- 共用檔案／介面與協調結果：catalog 新增可選 `hints: string[]`；沒有 hints 的既有 fixture 仍只顯示分類。
- 依賴任務：無。

## 驗收條件
- [ ] 線篩卡片依序顯示「數學」「質數篩」。
- [ ] 線段樹卡片依序顯示「資料結構」「區間修改」「區間和」。
- [ ] 目前所有公開範例都有適合的用途提示，且提示文字可用搜尋欄篩選。
- [ ] 沒有 `hints` 的舊 catalog 項目仍可正常顯示分類。

## 驗證計畫
- 子代理小驗證：JSON 與 JavaScript 語法；用途提示專用瀏覽器測試；入口版本測試；差異檢查。
- 主代理整合驗收：整合預覽檢視範例卡片標籤、搜尋及手機換行。
- 測試隔離方式：隨機埠服務、mock catalog 與獨立 headless Edge；不讀寫使用者投影片。

## 變更紀錄
- 2026-09-23：依使用者範例建立資料驅動的分類與用途提示。
