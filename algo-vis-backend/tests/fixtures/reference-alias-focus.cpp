#include <bits/stdc++.h>
using namespace std;

void inspect(vector<int>& arr, int n) {
  int value = arr[3];
  // @frame arr render heap with range(1,4)
  // @style arr[1:n] focus
}

int main() {
  vector<int> arr = {0, 4, 3, 2, 1};
  int n = 2;
  inspect(arr, n);
  // @frame arr render heap with range(1,4)
  // @style arr[1:n] focus
}
