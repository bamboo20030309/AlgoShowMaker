#include <bits/stdc++.h>
using namespace std;
int main() {
    vector<int> arr = {1,2,3};
    int total = 0;
    // @frame arr
    // @style arr[0:2] focus when value > 0
    total += arr[0];
    // @frame arr
    // @style arr[0:2] focus when value > 0
    total += arr[1];
    // @frame arr
    // @style arr[0:2] focus when value > 0
    total += arr[2];
    // @frame arr
    // @style arr[0:2] focus when value > 0
    cout << total;
}
