# 給開發／子代理的通知：驗證分級與測試分類

更新日期：2026-09-20。依使用者最新指示，本規則適用於所有開發代理，包括獨立 Codex 任務與主代理建立的子代理。

## 一、先判斷影響範圍，再選驗證

不要因為修改了 JavaScript、前端、slides.js 或投影片，就啟動完整驗證集。
依實際修改的函式、資料流與行為分類，不以檔名判斷是否涉及動畫。

| 層級 | 修改範圍 | 驗證方式 | 執行者 |
|---|---|---|---|
| V0 | 文件、靜態文字、純 CSS／圖示／版面 | 差異檢查；畫面有變動就做相關局部操作確認 | 開發代理 |
| V1 | 非動畫的前端、投影片元件、編輯、undo/redo、儲存／匯出邏輯 | 修改 JS 的語法檢查、局部操作確認；需要時只選直接相關測試檔。不得啟動演算法驗證集 | 開發代理 |
| V2 | 指令解析、trace/runtime/model、動畫繪圖、事件排程、style、camera、keep、播放或動畫設定還原 | 語法與差異檢查，加上相關分類內的最小測試檔／案例。涉及實際畫面時做最小隔離重現 | 開發代理 |
| V3 | 全部小測試、廣泛排序整合、跨介面與大規模演算法動畫 | 由主代理按整合變更決定完整或限定範圍的驗證 | 僅主代理 |

V0／V1 不需要啟用演算法驗證集，也不必為了完成純介面修正而 RUN 排序演算法。
但局部行為仍需核實，例如 LaTeX 匯入後顯示、undo/redo、投影片排序與儲存後重開。
純投影片修改若確實碰到動畫載入、trace 還原或播放設定，改歸 V2；不能因為是投影片檔案就忽略其影響。

## 二、子代理禁止使用的入口

子代理不得自行執行以下指令或透過其他方式啟動同等大範圍驗證：

- `npm run regression`
- `npm run regression:animation`
- `node scripts/regression.js`（包括 `--animation-only`、`--tests-only`）
- `npm test`、`node --test tests/*.test.js`，或自行列出全部測試檔
- `node scripts/animation-browser.js` 或直接呼叫 `runAnimationBrowser`
- `node --test tests/sorting.integration.test.js`
- 自建迴圈依序 RUN 多種排序、所有幀或三介面全套動畫，藉此繞過限制

上述限制不代表這些測試失效，而是由主代理集中執行。
設定 `ASM_ANIMATION_CASES` 也不解除限制：現有動畫入口除選定演算法外，仍先執行 cloud-storage-browser、slide-order-toggle、deck-import-repair。
需要大驗證時，在交付紀錄寫明原因與希望主代理驗證的案例。

## 三、檔案與報告在哪裡

所有路徑以代理自己的 worktree 為根，不要到其他代理或使用者主工作目錄執行。

| 內容 | 相對於 worktree 的路徑 |
|---|---|
| 小測試檔 | `algo-vis-backend/tests/*.test.js` |
| C++、JSON、asmdeck 測試輸入 | `algo-vis-backend/tests/fixtures/` |
| 共用 analyze／compile helper | `algo-vis-backend/tests/helpers/compile.js` |
| 測試說明 | `algo-vis-backend/tests/README.md` |
| 完整回歸入口 | `algo-vis-backend/scripts/regression.js` |
| 大規模瀏覽器動畫入口 | `algo-vis-backend/scripts/animation-browser.js` |
| 瀏覽器專項 helper | `algo-vis-backend/scripts/slide-order-browser.js`、`deck-import-browser.js`、`cloud-storage-browser.js` |
| 完整小測試執行紀錄 | `algo-vis-backend/test-results/regression.tap` |
| 大動畫報告、截圖與逐幀記錄 | `algo-vis-backend/test-results/animation/<時間>/`，包含 `summary.json` |
| 任務／交付文件 | `docs/agent-tasks/<任務代號>/task.md`、`delivery.md` |

主工作目錄的小測試絕對位置：

```text
C:\Users\user\Documents\Codex\2026-07-29\algoshowmaker-main-commit-d154dd5-slides-html\work\AlgoShowMaker\algo-vis-backend\tests
```

目前共有 94 個 `.test.js` 檔（含每幀事件／批次箭頭、一般結構註標、投影片元件框選與 Heap 複合格專項）。交接的 373 是當時執行的測試案例總數，不是 373 個檔案；
案例數會隨版本與動態產生的測試改變，本次沒有重跑全套以重新確認案例總數。
小測試混合靜態檢查、VM／替代 DOM、HTTP 編譯整合與專項瀏覽器，不代表全部都便宜或都驗證了實際像素。

## 四、小測試大致分類

下面是選檔索引，不是要求整類全跑。先挑最貼近錯誤的檔案；跨類才加上直接受影響的檔案。
不要不加判斷地跑某類所有測試，也不要反覆累積分類變成全套驗證。

### A：一般前端／編輯介面

適用：版面、手機介面、剪貼簿、提示選單、入口快取、文字設定。

- `tests/auth-center.browser.test.js`
- `tests/mobile-ui.test.js`
- `tests/editor-clipboard.test.js`
- `tests/directive-assist.test.js`
- `tests/entrypoints.test.js`
- `tests/object-inspector-font-size.test.js`
- `tests/algorithm-draft-storage.test.js`

### B：投影片元件／歷史

適用：LaTeX 匯入重繪、Fabric 文字編輯與 undo/redo、游標閃爍、code 元件關注行對齊。
code 元件配對轉場的定點核實屬 V2；一般元件操作屬 V1，不因此啟動演算法大驗證。

- `tests/structure-annotations.browser.test.js`
- `tests/code-focus-entry.browser.test.js`
- `tests/cursor-blink.browser.test.js`
- `tests/latex-refresh.browser.test.js`
- `tests/sample-latex-load.browser.test.js`
- `tests/slides-text-undo.browser.test.js`
- `tests/list-backspace.browser.test.js`
- `tests/text-resolution.browser.test.js`
- `tests/structure-length-zero.browser.test.js`
- `tests/widget-marquee-selection.browser.test.js`

### C：投影片儲存／雲端／匯出

適用：IndexedDB、雜湊、參照回收、上傳、容量與 asmdeck。

- `tests/slide-storage.test.js`
- `tests/slide-cloud-storage.test.js`
- `tests/cloud-content.test.js`
- `tests/asmdeck.test.js`
- `tests/http-payload-limits.test.js`
- `tests/deck-file-drop.browser.test.js`
- `tests/export-filename.browser.test.js`
- `tests/slide-library.test.js`
- `tests/library-folders.browser.test.js`

### D：動畫設定保存／來源識別

適用：投影片動畫設定還原、是否需重新 RUN、view 精簡。

- `tests/slide-animation-parity.test.js`
- `tests/provenance.test.js`
- `tests/view-source-compaction.test.js`
- `tests/event-settings-source.browser.test.js`

### E：指令／frame／綁定／位置

適用：preset、defaults、frame/object、place/layout、camera、text、arrow。

- `tests/preset-directives.test.js`
- `tests/defaults-directives.test.js`
- `tests/frame-renderer-options.integration.test.js`
- `tests/place-directives.test.js`
- `tests/layout-directives.test.js`
- `tests/camera-directives.test.js`
- `tests/text-object-bindings.test.js`
- `tests/drawing-loops.test.js`
- `tests/events-batch-arrows.test.js`
- `tests/events-batch-arrows.browser.test.js`
- `tests/automark.test.js`
- `tests/automark.browser.test.js`
- `tests/text-arrays.test.js`
- `tests/text-arrays.browser.test.js`
- `tests/arrow-directives.test.js`
- `tests/arrow-identity.test.js`
- `tests/heap-composite-segments.test.js`
- `tests/segment-tree-samples.test.js`

### F：runtime 事件／索引／資料

適用：比較、賦值、函式呼叫、range-for、迴圈摘要、序列操作、未初始化。

- `tests/assignment-indices.integration.test.js`
- `tests/function-call-event.integration.test.js`
- `tests/function-enter-event.integration.test.js`
- `tests/iteration-summary.integration.test.js`
- `tests/range-for-variables.integration.test.js`
- `tests/sequence-operations.integration.test.js`
- `tests/truthy-compare.integration.test.js`
- `tests/uninitialized-values.integration.test.js`
- `tests/empty-initial-render.test.js`
- `tests/index-only-assignment.test.js`

### G：指標／keep／生命週期／排程

適用：指標移動、讓位、進退場、遞迴角色、keep、checkpoint。

- `tests/heap-marker-assignment.integration.test.js`
- `tests/unresolved-markers.test.js`
- `tests/keep-directives.test.js`
- `tests/keep-arrow-layer.integration.test.js`
- `tests/scene-exit-entrance-order.test.js`
- `tests/outerframe-tween.test.js`

### H：style／分層／標籤

適用：background/highlight/point、value/index、未知值、動畫層與指標層。

- `tests/style-segments.integration.test.js`
- `tests/presented-style-values.integration.test.js`
- `tests/uninitialized-style.test.js`
- `tests/auto-fixed-playback.browser.test.js`
- `tests/events-fixed-state.test.js`
- `tests/events-fixed-state.browser.test.js`
- `tests/drawing-arrow-animation.browser.test.js`
- `tests/style-replay.browser.test.js`
- `tests/style-list.test.js`
- `tests/style-list.browser.test.js`
- `tests/style-layer.test.js`
- `tests/presentation-hints.test.js`
- `tests/animation-effect-layer.test.js`
- `tests/pointer-layer.test.js`
- `tests/index-label-growth.test.js`
- `tests/heap-composite-segments.browser.test.js`

### I：演算法程式碼呈現

適用：片段裁切、捲動、事件高亮、程式碼結構；不是一般 code 元件的通用測試。

- `tests/code-presentation.integration.test.js`
- `tests/code-exit-presentation.test.js`
- `tests/event-code-tree.test.js`
- `tests/euler-disabled-compare.integration.test.js`

### J：Studio／播放／鏡頭一致性

適用：事件開關、縮圖刷新、播放器與嵌入介面的一致性。

- `tests/event-defaults.test.js`
- `tests/studio-event-availability.test.js`
- `tests/playback-parity.integration.test.js`
- `tests/camera-embed-parity.test.js`

### K：驗證工具自身

適用：動畫斷言與除錯記錄器；只有改這些工具時才需選用。

- `tests/animation-assertions.test.js`
- `tests/trace-debug-recorder.test.js`

### L：後端／認證／執行保護

適用：JWT、API 認證、timeout/OLE 保護。

- `tests/jwt-config.test.js`
- `tests/auth.integration.test.js`
- `tests/execution-watchdog.test.js`

### M：廣泛排序整合（主代理專用）

適用：多種排序案例的共同 trace／事件契約。

- `tests/sorting.integration.test.js`

## 五、如何跑相關小驗證

在自己的 `algo-vis-backend` 目錄執行。

### 不需要 HTTP 服務的例子

```powershell
node --check public/slides.js
git diff --check
node --test tests/slide-storage.test.js tests/asmdeck.test.js
```

此例只適合有修改相關儲存／匯出行為時，不是每次投影片修改都跑。

### LaTeX 或文字歷史的專項瀏覽器

```powershell
node --test tests/latex-refresh.browser.test.js
node --test tests/slides-text-undo.browser.test.js
```

只選相關的一項。兩個檔案有自己的臨時服務啟動邏輯，執行前仍要查看當前版本。
LaTeX 可能需要 CDN 資源；資源或瀏覽器不可用時記錄環境阻塞，不刪除錯誤斷言。

### 同一檔案太大時，只選相關案例

先查實際測試名稱，再選取。例如：

```powershell
rg -n 'test\(' tests/unresolved-markers.test.js
node --test --test-name-pattern='same-cell marker reflow' tests/unresolved-markers.test.js
```

名稱篩選需確實命中並執行預期案例。零案例命中、全部 skip 或只有 exit code 0，不算修正通過。
篩選只限制案例執行，模組頂層程式仍會執行；先讀測試，不假設載入沒有副作用。
不要用名稱篩選刻意略過直接相關的失敗。

### 需要 HTTP／C++ 編譯的測試

不要直接讓測試使用預設 `localhost:3000`。很多整合檔透過 `helpers/compile.js` 使用
`ASM_TEST_BASE_URL`，未設定會回落到使用者的開發服務。
非 `.integration.test.js` 的檔案也可能有編譯案例，例如 defaults、keep、place、layout、arrow、text；
先讀檔案與 helper，按實際依賴啟動隔離服務。

在自己的後端目錄，用獨立終端啟動測試服務，選擇未占用的埠；以下 3101 僅為例子：

```powershell
$env:PORT = '3101'
$env:ASM_REGRESSION = '1'
$env:JWT_SECRET = node -e "process.stdout.write(require('node:crypto').randomBytes(32).toString('hex'))"
node server.js
```

另一個獨立終端，在同一 worktree 的後端目錄確認服務就緒後執行：

```powershell
$env:ASM_TEST_BASE_URL = 'http://127.0.0.1:3101'
node --test --test-concurrency=1 tests/assignment-indices.integration.test.js
```

JWT 認證整合測試還需要測試端與服務端使用相同的測試 secret；不得輸出 secret 到交付紀錄。
並行代理各用自己的埠，不使用這個例子的相同埠。
結束後只停止自己核對過的測試服務與瀏覽器，清除測試終端的環境設定，不停止全部 Node 程序。
背景啟動服務時使用隱藏視窗。

某些測試會在設定缺失時 skip。交付前檢查通過／失敗／skip 數量與案例名稱，不能把 skip 寫成通過。
使用替代資料庫的測試不代表實際 Mongo 或公開環境驗證完成。

## 六、交付時要回報什麼

將以下摘要加入 `delivery.md`：

```markdown
## 驗證分級與選擇
- 層級：V0／V1／V2
- 分類：A～L，可複選；無相關現有測試則註明
- 選擇依據：修改的行為與受影響資料流
- 執行的測試檔／名稱篩選：
- 驗證環境與隔離服務：
- 驗證版本、完整指令、結果與證據：
- 未執行的驗證及原因：
- 需要主代理做的 V3 驗證：無／具體案例與原因
```

不存在相關測試時，以最小重現與局部操作證據交付；必要時新增有價值的針對性測試，
不為純版面或低影響可逆變更建立只比對實作文字的測試。

非動畫任務可寫「未執行演算法驗證集：本次修改不涉及動畫」，這是依範圍選擇，不是測試失敗。
本次未執行完整驗證，不宣稱完整回歸通過。

## 七、主代理的責任

主代理先核實小驗證及差異，再逐一合併。純 V0／V1 整合不因合併就自動啟動演算法大驗證。
涉及 V2 的整合由主代理決定相關範圍或完整 V3，承擔最後演算法投影片實際驗證。
依賴擴大、修正失敗或新風險出現時才擴大驗證範圍；不得機械式重跑無關案例。

